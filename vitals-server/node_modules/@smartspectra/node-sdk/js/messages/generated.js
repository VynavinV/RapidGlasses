/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars*/
"use strict";

var $protobuf = require("protobufjs/minimal");

// Common aliases
var $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;

// Exported root namespace
var $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

$root.presage = (function() {

    /**
     * Namespace presage.
     * @exports presage
     * @namespace
     */
    var presage = {};

    presage.smartspectra = (function() {

        /**
         * Namespace smartspectra.
         * @memberof presage
         * @namespace
         */
        var smartspectra = {};

        smartspectra.Measurement = (function() {

            /**
             * Properties of a Measurement.
             * @memberof presage.smartspectra
             * @interface IMeasurement
             * @property {number|null} [value] The measured or estimated value
             * @property {boolean|null} [stable] Whether the measurement is considered stable/reliable
             * @property {number|Long|null} [timestamp] Absolute timestamp at which the measurement was taken, in microseconds, since Linux epoch
             */

            /**
             * Constructs a new Measurement.
             * @memberof presage.smartspectra
             * @classdesc Represents a single measurement with timestamp and stability information.
             * Used for various physiological measurements throughout the system.
             * @implements IMeasurement
             * @constructor
             * @param {presage.smartspectra.IMeasurement=} [properties] Properties to set
             */
            function Measurement(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * The measured or estimated value
             * @member {number} value
             * @memberof presage.smartspectra.Measurement
             * @instance
             */
            Measurement.prototype.value = 0;

            /**
             * Whether the measurement is considered stable/reliable
             * @member {boolean} stable
             * @memberof presage.smartspectra.Measurement
             * @instance
             */
            Measurement.prototype.stable = false;

            /**
             * Absolute timestamp at which the measurement was taken, in microseconds, since Linux epoch
             * @member {number|Long} timestamp
             * @memberof presage.smartspectra.Measurement
             * @instance
             */
            Measurement.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * Creates a new Measurement instance using the specified properties.
             * @function create
             * @memberof presage.smartspectra.Measurement
             * @static
             * @param {presage.smartspectra.IMeasurement=} [properties] Properties to set
             * @returns {presage.smartspectra.Measurement} Measurement instance
             */
            Measurement.create = function create(properties) {
                return new Measurement(properties);
            };

            /**
             * Encodes the specified Measurement message. Does not implicitly {@link presage.smartspectra.Measurement.verify|verify} messages.
             * @function encode
             * @memberof presage.smartspectra.Measurement
             * @static
             * @param {presage.smartspectra.IMeasurement} message Measurement message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Measurement.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                    writer.uint32(/* id 1, wireType 5 =*/13).float(message.value);
                if (message.stable != null && Object.hasOwnProperty.call(message, "stable"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.stable);
                if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.timestamp);
                return writer;
            };

            /**
             * Encodes the specified Measurement message, length delimited. Does not implicitly {@link presage.smartspectra.Measurement.verify|verify} messages.
             * @function encodeDelimited
             * @memberof presage.smartspectra.Measurement
             * @static
             * @param {presage.smartspectra.IMeasurement} message Measurement message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Measurement.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a Measurement message from the specified reader or buffer.
             * @function decode
             * @memberof presage.smartspectra.Measurement
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {presage.smartspectra.Measurement} Measurement
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Measurement.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = new $root.presage.smartspectra.Measurement();
                while (reader.pos < end) {
                    var tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.value = reader.float();
                            break;
                        }
                    case 2: {
                            message.stable = reader.bool();
                            break;
                        }
                    case 3: {
                            message.timestamp = reader.int64();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Measurement message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof presage.smartspectra.Measurement
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {presage.smartspectra.Measurement} Measurement
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Measurement.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Measurement message.
             * @function verify
             * @memberof presage.smartspectra.Measurement
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Measurement.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.value != null && message.hasOwnProperty("value"))
                    if (typeof message.value !== "number")
                        return "value: number expected";
                if (message.stable != null && message.hasOwnProperty("stable"))
                    if (typeof message.stable !== "boolean")
                        return "stable: boolean expected";
                if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                    if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                        return "timestamp: integer|Long expected";
                return null;
            };

            /**
             * Creates a Measurement message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof presage.smartspectra.Measurement
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {presage.smartspectra.Measurement} Measurement
             */
            Measurement.fromObject = function fromObject(object, long) {
                if (object instanceof $root.presage.smartspectra.Measurement)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var message = new $root.presage.smartspectra.Measurement();
                if (object.value != null)
                    message.value = Number(object.value);
                if (object.stable != null)
                    message.stable = Boolean(object.stable);
                if (object.timestamp != null)
                    if ($util.Long)
                        message.timestamp = $util.Long.fromValue(object.timestamp, false);
                    else if (typeof object.timestamp === "string")
                        message.timestamp = parseInt(object.timestamp, 10);
                    else if (typeof object.timestamp === "number")
                        message.timestamp = object.timestamp;
                    else if (typeof object.timestamp === "object")
                        message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a Measurement message. Also converts values to other types if specified.
             * @function toObject
             * @memberof presage.smartspectra.Measurement
             * @static
             * @param {presage.smartspectra.Measurement} message Measurement
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Measurement.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.value = 0;
                    object.stable = false;
                    if ($util.Long) {
                        var long = new $util.Long(0, 0, false);
                        object.timestamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.timestamp = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                }
                if (message.value != null && message.hasOwnProperty("value"))
                    object.value = options.json && !isFinite(message.value) ? String(message.value) : message.value;
                if (message.stable != null && message.hasOwnProperty("stable"))
                    object.stable = message.stable;
                if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.timestamp === "number")
                        object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                    else
                        object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
                return object;
            };

            /**
             * Converts this Measurement to JSON.
             * @function toJSON
             * @memberof presage.smartspectra.Measurement
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Measurement.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Measurement
             * @function getTypeUrl
             * @memberof presage.smartspectra.Measurement
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Measurement.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/presage.smartspectra.Measurement";
            };

            return Measurement;
        })();

        smartspectra.DetectionStatus = (function() {

            /**
             * Properties of a DetectionStatus.
             * @memberof presage.smartspectra
             * @interface IDetectionStatus
             * @property {boolean|null} [detected] Whether the feature/state was detected
             * @property {boolean|null} [stable] Whether the detection is considered stable/reliable
             * @property {number|Long|null} [timestamp] Absolute timestamp at which the detection status was updated, in microseconds, since Linux epoch
             */

            /**
             * Constructs a new DetectionStatus.
             * @memberof presage.smartspectra
             * @classdesc Represents detection status with timestamp information.
             * Used to track whether a particular physiological feature or state is detected.
             * @implements IDetectionStatus
             * @constructor
             * @param {presage.smartspectra.IDetectionStatus=} [properties] Properties to set
             */
            function DetectionStatus(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Whether the feature/state was detected
             * @member {boolean} detected
             * @memberof presage.smartspectra.DetectionStatus
             * @instance
             */
            DetectionStatus.prototype.detected = false;

            /**
             * Whether the detection is considered stable/reliable
             * @member {boolean} stable
             * @memberof presage.smartspectra.DetectionStatus
             * @instance
             */
            DetectionStatus.prototype.stable = false;

            /**
             * Absolute timestamp at which the detection status was updated, in microseconds, since Linux epoch
             * @member {number|Long} timestamp
             * @memberof presage.smartspectra.DetectionStatus
             * @instance
             */
            DetectionStatus.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * Creates a new DetectionStatus instance using the specified properties.
             * @function create
             * @memberof presage.smartspectra.DetectionStatus
             * @static
             * @param {presage.smartspectra.IDetectionStatus=} [properties] Properties to set
             * @returns {presage.smartspectra.DetectionStatus} DetectionStatus instance
             */
            DetectionStatus.create = function create(properties) {
                return new DetectionStatus(properties);
            };

            /**
             * Encodes the specified DetectionStatus message. Does not implicitly {@link presage.smartspectra.DetectionStatus.verify|verify} messages.
             * @function encode
             * @memberof presage.smartspectra.DetectionStatus
             * @static
             * @param {presage.smartspectra.IDetectionStatus} message DetectionStatus message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DetectionStatus.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.detected != null && Object.hasOwnProperty.call(message, "detected"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.detected);
                if (message.stable != null && Object.hasOwnProperty.call(message, "stable"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.stable);
                if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.timestamp);
                return writer;
            };

            /**
             * Encodes the specified DetectionStatus message, length delimited. Does not implicitly {@link presage.smartspectra.DetectionStatus.verify|verify} messages.
             * @function encodeDelimited
             * @memberof presage.smartspectra.DetectionStatus
             * @static
             * @param {presage.smartspectra.IDetectionStatus} message DetectionStatus message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DetectionStatus.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a DetectionStatus message from the specified reader or buffer.
             * @function decode
             * @memberof presage.smartspectra.DetectionStatus
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {presage.smartspectra.DetectionStatus} DetectionStatus
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DetectionStatus.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = new $root.presage.smartspectra.DetectionStatus();
                while (reader.pos < end) {
                    var tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.detected = reader.bool();
                            break;
                        }
                    case 2: {
                            message.stable = reader.bool();
                            break;
                        }
                    case 3: {
                            message.timestamp = reader.int64();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a DetectionStatus message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof presage.smartspectra.DetectionStatus
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {presage.smartspectra.DetectionStatus} DetectionStatus
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DetectionStatus.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DetectionStatus message.
             * @function verify
             * @memberof presage.smartspectra.DetectionStatus
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DetectionStatus.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.detected != null && message.hasOwnProperty("detected"))
                    if (typeof message.detected !== "boolean")
                        return "detected: boolean expected";
                if (message.stable != null && message.hasOwnProperty("stable"))
                    if (typeof message.stable !== "boolean")
                        return "stable: boolean expected";
                if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                    if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                        return "timestamp: integer|Long expected";
                return null;
            };

            /**
             * Creates a DetectionStatus message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof presage.smartspectra.DetectionStatus
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {presage.smartspectra.DetectionStatus} DetectionStatus
             */
            DetectionStatus.fromObject = function fromObject(object, long) {
                if (object instanceof $root.presage.smartspectra.DetectionStatus)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var message = new $root.presage.smartspectra.DetectionStatus();
                if (object.detected != null)
                    message.detected = Boolean(object.detected);
                if (object.stable != null)
                    message.stable = Boolean(object.stable);
                if (object.timestamp != null)
                    if ($util.Long)
                        message.timestamp = $util.Long.fromValue(object.timestamp, false);
                    else if (typeof object.timestamp === "string")
                        message.timestamp = parseInt(object.timestamp, 10);
                    else if (typeof object.timestamp === "number")
                        message.timestamp = object.timestamp;
                    else if (typeof object.timestamp === "object")
                        message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a DetectionStatus message. Also converts values to other types if specified.
             * @function toObject
             * @memberof presage.smartspectra.DetectionStatus
             * @static
             * @param {presage.smartspectra.DetectionStatus} message DetectionStatus
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DetectionStatus.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.detected = false;
                    object.stable = false;
                    if ($util.Long) {
                        var long = new $util.Long(0, 0, false);
                        object.timestamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.timestamp = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                }
                if (message.detected != null && message.hasOwnProperty("detected"))
                    object.detected = message.detected;
                if (message.stable != null && message.hasOwnProperty("stable"))
                    object.stable = message.stable;
                if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.timestamp === "number")
                        object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                    else
                        object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
                return object;
            };

            /**
             * Converts this DetectionStatus to JSON.
             * @function toJSON
             * @memberof presage.smartspectra.DetectionStatus
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DetectionStatus.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for DetectionStatus
             * @function getTypeUrl
             * @memberof presage.smartspectra.DetectionStatus
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            DetectionStatus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/presage.smartspectra.DetectionStatus";
            };

            return DetectionStatus;
        })();

        smartspectra.MeasurementWithConfidence = (function() {

            /**
             * Properties of a MeasurementWithConfidence.
             * @memberof presage.smartspectra
             * @interface IMeasurementWithConfidence
             * @property {number|null} [value] The measured value
             * @property {boolean|null} [stable] Whether the measurement is considered stable/reliable
             * @property {number|null} [confidence] Confidence score for the measurement, expressed as a percentage in the range [0.0, 100.0]
             * @property {number|Long|null} [timestamp] Absolute timestamp at which the measurement was taken, in microseconds, since Linux epoch
             */

            /**
             * Constructs a new MeasurementWithConfidence.
             * @memberof presage.smartspectra
             * @classdesc Represents a measurement with an associated confidence score.
             * Extends basic measurement with confidence information for quality assessment.
             * @implements IMeasurementWithConfidence
             * @constructor
             * @param {presage.smartspectra.IMeasurementWithConfidence=} [properties] Properties to set
             */
            function MeasurementWithConfidence(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * The measured value
             * @member {number} value
             * @memberof presage.smartspectra.MeasurementWithConfidence
             * @instance
             */
            MeasurementWithConfidence.prototype.value = 0;

            /**
             * Whether the measurement is considered stable/reliable
             * @member {boolean} stable
             * @memberof presage.smartspectra.MeasurementWithConfidence
             * @instance
             */
            MeasurementWithConfidence.prototype.stable = false;

            /**
             * Confidence score for the measurement, expressed as a percentage in the range [0.0, 100.0]
             * @member {number} confidence
             * @memberof presage.smartspectra.MeasurementWithConfidence
             * @instance
             */
            MeasurementWithConfidence.prototype.confidence = 0;

            /**
             * Absolute timestamp at which the measurement was taken, in microseconds, since Linux epoch
             * @member {number|Long} timestamp
             * @memberof presage.smartspectra.MeasurementWithConfidence
             * @instance
             */
            MeasurementWithConfidence.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * Creates a new MeasurementWithConfidence instance using the specified properties.
             * @function create
             * @memberof presage.smartspectra.MeasurementWithConfidence
             * @static
             * @param {presage.smartspectra.IMeasurementWithConfidence=} [properties] Properties to set
             * @returns {presage.smartspectra.MeasurementWithConfidence} MeasurementWithConfidence instance
             */
            MeasurementWithConfidence.create = function create(properties) {
                return new MeasurementWithConfidence(properties);
            };

            /**
             * Encodes the specified MeasurementWithConfidence message. Does not implicitly {@link presage.smartspectra.MeasurementWithConfidence.verify|verify} messages.
             * @function encode
             * @memberof presage.smartspectra.MeasurementWithConfidence
             * @static
             * @param {presage.smartspectra.IMeasurementWithConfidence} message MeasurementWithConfidence message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MeasurementWithConfidence.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                    writer.uint32(/* id 1, wireType 5 =*/13).float(message.value);
                if (message.stable != null && Object.hasOwnProperty.call(message, "stable"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.stable);
                if (message.confidence != null && Object.hasOwnProperty.call(message, "confidence"))
                    writer.uint32(/* id 3, wireType 5 =*/29).float(message.confidence);
                if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int64(message.timestamp);
                return writer;
            };

            /**
             * Encodes the specified MeasurementWithConfidence message, length delimited. Does not implicitly {@link presage.smartspectra.MeasurementWithConfidence.verify|verify} messages.
             * @function encodeDelimited
             * @memberof presage.smartspectra.MeasurementWithConfidence
             * @static
             * @param {presage.smartspectra.IMeasurementWithConfidence} message MeasurementWithConfidence message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MeasurementWithConfidence.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a MeasurementWithConfidence message from the specified reader or buffer.
             * @function decode
             * @memberof presage.smartspectra.MeasurementWithConfidence
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {presage.smartspectra.MeasurementWithConfidence} MeasurementWithConfidence
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MeasurementWithConfidence.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = new $root.presage.smartspectra.MeasurementWithConfidence();
                while (reader.pos < end) {
                    var tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.value = reader.float();
                            break;
                        }
                    case 2: {
                            message.stable = reader.bool();
                            break;
                        }
                    case 3: {
                            message.confidence = reader.float();
                            break;
                        }
                    case 4: {
                            message.timestamp = reader.int64();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a MeasurementWithConfidence message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof presage.smartspectra.MeasurementWithConfidence
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {presage.smartspectra.MeasurementWithConfidence} MeasurementWithConfidence
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MeasurementWithConfidence.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MeasurementWithConfidence message.
             * @function verify
             * @memberof presage.smartspectra.MeasurementWithConfidence
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MeasurementWithConfidence.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.value != null && message.hasOwnProperty("value"))
                    if (typeof message.value !== "number")
                        return "value: number expected";
                if (message.stable != null && message.hasOwnProperty("stable"))
                    if (typeof message.stable !== "boolean")
                        return "stable: boolean expected";
                if (message.confidence != null && message.hasOwnProperty("confidence"))
                    if (typeof message.confidence !== "number")
                        return "confidence: number expected";
                if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                    if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                        return "timestamp: integer|Long expected";
                return null;
            };

            /**
             * Creates a MeasurementWithConfidence message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof presage.smartspectra.MeasurementWithConfidence
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {presage.smartspectra.MeasurementWithConfidence} MeasurementWithConfidence
             */
            MeasurementWithConfidence.fromObject = function fromObject(object, long) {
                if (object instanceof $root.presage.smartspectra.MeasurementWithConfidence)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var message = new $root.presage.smartspectra.MeasurementWithConfidence();
                if (object.value != null)
                    message.value = Number(object.value);
                if (object.stable != null)
                    message.stable = Boolean(object.stable);
                if (object.confidence != null)
                    message.confidence = Number(object.confidence);
                if (object.timestamp != null)
                    if ($util.Long)
                        message.timestamp = $util.Long.fromValue(object.timestamp, false);
                    else if (typeof object.timestamp === "string")
                        message.timestamp = parseInt(object.timestamp, 10);
                    else if (typeof object.timestamp === "number")
                        message.timestamp = object.timestamp;
                    else if (typeof object.timestamp === "object")
                        message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a MeasurementWithConfidence message. Also converts values to other types if specified.
             * @function toObject
             * @memberof presage.smartspectra.MeasurementWithConfidence
             * @static
             * @param {presage.smartspectra.MeasurementWithConfidence} message MeasurementWithConfidence
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MeasurementWithConfidence.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.value = 0;
                    object.stable = false;
                    object.confidence = 0;
                    if ($util.Long) {
                        var long = new $util.Long(0, 0, false);
                        object.timestamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.timestamp = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                }
                if (message.value != null && message.hasOwnProperty("value"))
                    object.value = options.json && !isFinite(message.value) ? String(message.value) : message.value;
                if (message.stable != null && message.hasOwnProperty("stable"))
                    object.stable = message.stable;
                if (message.confidence != null && message.hasOwnProperty("confidence"))
                    object.confidence = options.json && !isFinite(message.confidence) ? String(message.confidence) : message.confidence;
                if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.timestamp === "number")
                        object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                    else
                        object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
                return object;
            };

            /**
             * Converts this MeasurementWithConfidence to JSON.
             * @function toJSON
             * @memberof presage.smartspectra.MeasurementWithConfidence
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MeasurementWithConfidence.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MeasurementWithConfidence
             * @function getTypeUrl
             * @memberof presage.smartspectra.MeasurementWithConfidence
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MeasurementWithConfidence.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/presage.smartspectra.MeasurementWithConfidence";
            };

            return MeasurementWithConfidence;
        })();

        /**
         * Enumerates the supported facial expression types output by the model.
         * @name presage.smartspectra.ExpressionType
         * @enum {number}
         * @property {number} UNSPECIFIED=0 Expression is unspecified or unknown
         * @property {number} ANGRY=1 Angry expression
         * @property {number} CONTEMPT=2 Contempt expression
         * @property {number} DISGUST=3 Disgust expression
         * @property {number} FEAR=4 Fear expression
         * @property {number} HAPPY=5 Happy expression
         * @property {number} NEUTRAL=6 Neutral expression
         * @property {number} SAD=7 Sad expression
         * @property {number} SURPRISE=8 Surprise expression
         */
        smartspectra.ExpressionType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNSPECIFIED"] = 0;
            values[valuesById[1] = "ANGRY"] = 1;
            values[valuesById[2] = "CONTEMPT"] = 2;
            values[valuesById[3] = "DISGUST"] = 3;
            values[valuesById[4] = "FEAR"] = 4;
            values[valuesById[5] = "HAPPY"] = 5;
            values[valuesById[6] = "NEUTRAL"] = 6;
            values[valuesById[7] = "SAD"] = 7;
            values[valuesById[8] = "SURPRISE"] = 8;
            return values;
        })();

        smartspectra.ExpressionScore = (function() {

            /**
             * Properties of an ExpressionScore.
             * @memberof presage.smartspectra
             * @interface IExpressionScore
             * @property {presage.smartspectra.ExpressionType|null} [type] Expression type identifier
             * @property {number|null} [confidence] Confidence score for the expression, expressed as a percentage in the range [0.0, 100.0]
             */

            /**
             * Constructs a new ExpressionScore.
             * @memberof presage.smartspectra
             * @classdesc Associates an expression type with its confidence score.
             * @implements IExpressionScore
             * @constructor
             * @param {presage.smartspectra.IExpressionScore=} [properties] Properties to set
             */
            function ExpressionScore(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Expression type identifier
             * @member {presage.smartspectra.ExpressionType} type
             * @memberof presage.smartspectra.ExpressionScore
             * @instance
             */
            ExpressionScore.prototype.type = 0;

            /**
             * Confidence score for the expression, expressed as a percentage in the range [0.0, 100.0]
             * @member {number} confidence
             * @memberof presage.smartspectra.ExpressionScore
             * @instance
             */
            ExpressionScore.prototype.confidence = 0;

            /**
             * Creates a new ExpressionScore instance using the specified properties.
             * @function create
             * @memberof presage.smartspectra.ExpressionScore
             * @static
             * @param {presage.smartspectra.IExpressionScore=} [properties] Properties to set
             * @returns {presage.smartspectra.ExpressionScore} ExpressionScore instance
             */
            ExpressionScore.create = function create(properties) {
                return new ExpressionScore(properties);
            };

            /**
             * Encodes the specified ExpressionScore message. Does not implicitly {@link presage.smartspectra.ExpressionScore.verify|verify} messages.
             * @function encode
             * @memberof presage.smartspectra.ExpressionScore
             * @static
             * @param {presage.smartspectra.IExpressionScore} message ExpressionScore message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ExpressionScore.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
                if (message.confidence != null && Object.hasOwnProperty.call(message, "confidence"))
                    writer.uint32(/* id 2, wireType 5 =*/21).float(message.confidence);
                return writer;
            };

            /**
             * Encodes the specified ExpressionScore message, length delimited. Does not implicitly {@link presage.smartspectra.ExpressionScore.verify|verify} messages.
             * @function encodeDelimited
             * @memberof presage.smartspectra.ExpressionScore
             * @static
             * @param {presage.smartspectra.IExpressionScore} message ExpressionScore message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ExpressionScore.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes an ExpressionScore message from the specified reader or buffer.
             * @function decode
             * @memberof presage.smartspectra.ExpressionScore
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {presage.smartspectra.ExpressionScore} ExpressionScore
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ExpressionScore.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = new $root.presage.smartspectra.ExpressionScore();
                while (reader.pos < end) {
                    var tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.type = reader.int32();
                            break;
                        }
                    case 2: {
                            message.confidence = reader.float();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an ExpressionScore message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof presage.smartspectra.ExpressionScore
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {presage.smartspectra.ExpressionScore} ExpressionScore
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ExpressionScore.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an ExpressionScore message.
             * @function verify
             * @memberof presage.smartspectra.ExpressionScore
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ExpressionScore.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.type != null && message.hasOwnProperty("type"))
                    switch (message.type) {
                    default:
                        return "type: enum value expected";
                    case 0:
                    case 1:
                    case 2:
                    case 3:
                    case 4:
                    case 5:
                    case 6:
                    case 7:
                    case 8:
                        break;
                    }
                if (message.confidence != null && message.hasOwnProperty("confidence"))
                    if (typeof message.confidence !== "number")
                        return "confidence: number expected";
                return null;
            };

            /**
             * Creates an ExpressionScore message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof presage.smartspectra.ExpressionScore
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {presage.smartspectra.ExpressionScore} ExpressionScore
             */
            ExpressionScore.fromObject = function fromObject(object, long) {
                if (object instanceof $root.presage.smartspectra.ExpressionScore)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var message = new $root.presage.smartspectra.ExpressionScore();
                switch (object.type) {
                default:
                    if (typeof object.type === "number") {
                        message.type = object.type;
                        break;
                    }
                    break;
                case "UNSPECIFIED":
                case 0:
                    message.type = 0;
                    break;
                case "ANGRY":
                case 1:
                    message.type = 1;
                    break;
                case "CONTEMPT":
                case 2:
                    message.type = 2;
                    break;
                case "DISGUST":
                case 3:
                    message.type = 3;
                    break;
                case "FEAR":
                case 4:
                    message.type = 4;
                    break;
                case "HAPPY":
                case 5:
                    message.type = 5;
                    break;
                case "NEUTRAL":
                case 6:
                    message.type = 6;
                    break;
                case "SAD":
                case 7:
                    message.type = 7;
                    break;
                case "SURPRISE":
                case 8:
                    message.type = 8;
                    break;
                }
                if (object.confidence != null)
                    message.confidence = Number(object.confidence);
                return message;
            };

            /**
             * Creates a plain object from an ExpressionScore message. Also converts values to other types if specified.
             * @function toObject
             * @memberof presage.smartspectra.ExpressionScore
             * @static
             * @param {presage.smartspectra.ExpressionScore} message ExpressionScore
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ExpressionScore.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.type = options.enums === String ? "UNSPECIFIED" : 0;
                    object.confidence = 0;
                }
                if (message.type != null && message.hasOwnProperty("type"))
                    object.type = options.enums === String ? $root.presage.smartspectra.ExpressionType[message.type] === undefined ? message.type : $root.presage.smartspectra.ExpressionType[message.type] : message.type;
                if (message.confidence != null && message.hasOwnProperty("confidence"))
                    object.confidence = options.json && !isFinite(message.confidence) ? String(message.confidence) : message.confidence;
                return object;
            };

            /**
             * Converts this ExpressionScore to JSON.
             * @function toJSON
             * @memberof presage.smartspectra.ExpressionScore
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ExpressionScore.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ExpressionScore
             * @function getTypeUrl
             * @memberof presage.smartspectra.ExpressionScore
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ExpressionScore.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/presage.smartspectra.ExpressionScore";
            };

            return ExpressionScore;
        })();

        smartspectra.Expression = (function() {

            /**
             * Properties of an Expression.
             * @memberof presage.smartspectra
             * @interface IExpression
             * @property {boolean|null} [stable] Whether the detection is considered stable/reliable
             * @property {number|Long|null} [timestamp] Absolute timestamp at which the expression was detected, in microseconds, since Linux epoch
             * @property {Array.<presage.smartspectra.IExpressionScore>|null} [scores] Confidence distribution across all expression types
             */

            /**
             * Constructs a new Expression.
             * @memberof presage.smartspectra
             * @classdesc Represents a detected expression with metadata.
             * Used for facial expression analysis and emotion detection.
             * @implements IExpression
             * @constructor
             * @param {presage.smartspectra.IExpression=} [properties] Properties to set
             */
            function Expression(properties) {
                this.scores = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Whether the detection is considered stable/reliable
             * @member {boolean} stable
             * @memberof presage.smartspectra.Expression
             * @instance
             */
            Expression.prototype.stable = false;

            /**
             * Absolute timestamp at which the expression was detected, in microseconds, since Linux epoch
             * @member {number|Long} timestamp
             * @memberof presage.smartspectra.Expression
             * @instance
             */
            Expression.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * Confidence distribution across all expression types
             * @member {Array.<presage.smartspectra.IExpressionScore>} scores
             * @memberof presage.smartspectra.Expression
             * @instance
             */
            Expression.prototype.scores = $util.emptyArray;

            /**
             * Creates a new Expression instance using the specified properties.
             * @function create
             * @memberof presage.smartspectra.Expression
             * @static
             * @param {presage.smartspectra.IExpression=} [properties] Properties to set
             * @returns {presage.smartspectra.Expression} Expression instance
             */
            Expression.create = function create(properties) {
                return new Expression(properties);
            };

            /**
             * Encodes the specified Expression message. Does not implicitly {@link presage.smartspectra.Expression.verify|verify} messages.
             * @function encode
             * @memberof presage.smartspectra.Expression
             * @static
             * @param {presage.smartspectra.IExpression} message Expression message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Expression.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.stable != null && Object.hasOwnProperty.call(message, "stable"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.stable);
                if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.timestamp);
                if (message.scores != null && message.scores.length)
                    for (var i = 0; i < message.scores.length; ++i)
                        $root.presage.smartspectra.ExpressionScore.encode(message.scores[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified Expression message, length delimited. Does not implicitly {@link presage.smartspectra.Expression.verify|verify} messages.
             * @function encodeDelimited
             * @memberof presage.smartspectra.Expression
             * @static
             * @param {presage.smartspectra.IExpression} message Expression message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Expression.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes an Expression message from the specified reader or buffer.
             * @function decode
             * @memberof presage.smartspectra.Expression
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {presage.smartspectra.Expression} Expression
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Expression.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = new $root.presage.smartspectra.Expression();
                while (reader.pos < end) {
                    var tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.stable = reader.bool();
                            break;
                        }
                    case 2: {
                            message.timestamp = reader.int64();
                            break;
                        }
                    case 3: {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            message.scores.push($root.presage.smartspectra.ExpressionScore.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an Expression message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof presage.smartspectra.Expression
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {presage.smartspectra.Expression} Expression
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Expression.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an Expression message.
             * @function verify
             * @memberof presage.smartspectra.Expression
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Expression.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.stable != null && message.hasOwnProperty("stable"))
                    if (typeof message.stable !== "boolean")
                        return "stable: boolean expected";
                if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                    if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                        return "timestamp: integer|Long expected";
                if (message.scores != null && message.hasOwnProperty("scores")) {
                    if (!Array.isArray(message.scores))
                        return "scores: array expected";
                    for (var i = 0; i < message.scores.length; ++i) {
                        var error = $root.presage.smartspectra.ExpressionScore.verify(message.scores[i], long + 1);
                        if (error)
                            return "scores." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an Expression message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof presage.smartspectra.Expression
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {presage.smartspectra.Expression} Expression
             */
            Expression.fromObject = function fromObject(object, long) {
                if (object instanceof $root.presage.smartspectra.Expression)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var message = new $root.presage.smartspectra.Expression();
                if (object.stable != null)
                    message.stable = Boolean(object.stable);
                if (object.timestamp != null)
                    if ($util.Long)
                        message.timestamp = $util.Long.fromValue(object.timestamp, false);
                    else if (typeof object.timestamp === "string")
                        message.timestamp = parseInt(object.timestamp, 10);
                    else if (typeof object.timestamp === "number")
                        message.timestamp = object.timestamp;
                    else if (typeof object.timestamp === "object")
                        message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
                if (object.scores) {
                    if (!Array.isArray(object.scores))
                        throw TypeError(".presage.smartspectra.Expression.scores: array expected");
                    message.scores = [];
                    for (var i = 0; i < object.scores.length; ++i) {
                        if (typeof object.scores[i] !== "object")
                            throw TypeError(".presage.smartspectra.Expression.scores: object expected");
                        message.scores[i] = $root.presage.smartspectra.ExpressionScore.fromObject(object.scores[i], long + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from an Expression message. Also converts values to other types if specified.
             * @function toObject
             * @memberof presage.smartspectra.Expression
             * @static
             * @param {presage.smartspectra.Expression} message Expression
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Expression.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.scores = [];
                if (options.defaults) {
                    object.stable = false;
                    if ($util.Long) {
                        var long = new $util.Long(0, 0, false);
                        object.timestamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.timestamp = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                }
                if (message.stable != null && message.hasOwnProperty("stable"))
                    object.stable = message.stable;
                if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.timestamp === "number")
                        object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                    else
                        object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
                if (message.scores && message.scores.length) {
                    object.scores = [];
                    for (var j = 0; j < message.scores.length; ++j)
                        object.scores[j] = $root.presage.smartspectra.ExpressionScore.toObject(message.scores[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this Expression to JSON.
             * @function toJSON
             * @memberof presage.smartspectra.Expression
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Expression.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Expression
             * @function getTypeUrl
             * @memberof presage.smartspectra.Expression
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Expression.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/presage.smartspectra.Expression";
            };

            return Expression;
        })();

        smartspectra.Hrv = (function() {

            /**
             * Properties of a Hrv.
             * @memberof presage.smartspectra
             * @interface IHrv
             * @property {number|null} [rmssd] root mean square of successive differences between normal heartbeats
             * @property {number|null} [meanNn] mean normal-to-normal (NN) interval length
             * @property {number|null} [sdnn] Standard Deviation of normal-to-normal (NN) Intervals
             * @property {number|null} [baevsky] Baevsky's Stress Index, in 1/ms^2 (inverse-square milliseconds)
             * @property {number|Long|null} [timestamp] Absolute timestamp at which the HRV measurement was taken, in microseconds, since Linux epoch
             * @property {number|null} [confidence] Confidence score for the HRV measurement, expressed as a percentage in the range [0.0, 100.0]
             */

            /**
             * Constructs a new Hrv.
             * @memberof presage.smartspectra
             * @classdesc Represents a single HRV measurement computed over some period of pulse data.
             * @implements IHrv
             * @constructor
             * @param {presage.smartspectra.IHrv=} [properties] Properties to set
             */
            function Hrv(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * root mean square of successive differences between normal heartbeats
             * @member {number} rmssd
             * @memberof presage.smartspectra.Hrv
             * @instance
             */
            Hrv.prototype.rmssd = 0;

            /**
             * mean normal-to-normal (NN) interval length
             * @member {number} meanNn
             * @memberof presage.smartspectra.Hrv
             * @instance
             */
            Hrv.prototype.meanNn = 0;

            /**
             * Standard Deviation of normal-to-normal (NN) Intervals
             * @member {number} sdnn
             * @memberof presage.smartspectra.Hrv
             * @instance
             */
            Hrv.prototype.sdnn = 0;

            /**
             * Baevsky's Stress Index, in 1/ms^2 (inverse-square milliseconds)
             * @member {number} baevsky
             * @memberof presage.smartspectra.Hrv
             * @instance
             */
            Hrv.prototype.baevsky = 0;

            /**
             * Absolute timestamp at which the HRV measurement was taken, in microseconds, since Linux epoch
             * @member {number|Long} timestamp
             * @memberof presage.smartspectra.Hrv
             * @instance
             */
            Hrv.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * Confidence score for the HRV measurement, expressed as a percentage in the range [0.0, 100.0]
             * @member {number} confidence
             * @memberof presage.smartspectra.Hrv
             * @instance
             */
            Hrv.prototype.confidence = 0;

            /**
             * Creates a new Hrv instance using the specified properties.
             * @function create
             * @memberof presage.smartspectra.Hrv
             * @static
             * @param {presage.smartspectra.IHrv=} [properties] Properties to set
             * @returns {presage.smartspectra.Hrv} Hrv instance
             */
            Hrv.create = function create(properties) {
                return new Hrv(properties);
            };

            /**
             * Encodes the specified Hrv message. Does not implicitly {@link presage.smartspectra.Hrv.verify|verify} messages.
             * @function encode
             * @memberof presage.smartspectra.Hrv
             * @static
             * @param {presage.smartspectra.IHrv} message Hrv message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Hrv.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.rmssd != null && Object.hasOwnProperty.call(message, "rmssd"))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.rmssd);
                if (message.meanNn != null && Object.hasOwnProperty.call(message, "meanNn"))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.meanNn);
                if (message.sdnn != null && Object.hasOwnProperty.call(message, "sdnn"))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.sdnn);
                if (message.baevsky != null && Object.hasOwnProperty.call(message, "baevsky"))
                    writer.uint32(/* id 4, wireType 1 =*/33).double(message.baevsky);
                if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.timestamp);
                if (message.confidence != null && Object.hasOwnProperty.call(message, "confidence"))
                    writer.uint32(/* id 6, wireType 5 =*/53).float(message.confidence);
                return writer;
            };

            /**
             * Encodes the specified Hrv message, length delimited. Does not implicitly {@link presage.smartspectra.Hrv.verify|verify} messages.
             * @function encodeDelimited
             * @memberof presage.smartspectra.Hrv
             * @static
             * @param {presage.smartspectra.IHrv} message Hrv message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Hrv.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a Hrv message from the specified reader or buffer.
             * @function decode
             * @memberof presage.smartspectra.Hrv
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {presage.smartspectra.Hrv} Hrv
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Hrv.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = new $root.presage.smartspectra.Hrv();
                while (reader.pos < end) {
                    var tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.rmssd = reader.double();
                            break;
                        }
                    case 2: {
                            message.meanNn = reader.double();
                            break;
                        }
                    case 3: {
                            message.sdnn = reader.double();
                            break;
                        }
                    case 4: {
                            message.baevsky = reader.double();
                            break;
                        }
                    case 5: {
                            message.timestamp = reader.int64();
                            break;
                        }
                    case 6: {
                            message.confidence = reader.float();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Hrv message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof presage.smartspectra.Hrv
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {presage.smartspectra.Hrv} Hrv
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Hrv.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Hrv message.
             * @function verify
             * @memberof presage.smartspectra.Hrv
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Hrv.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.rmssd != null && message.hasOwnProperty("rmssd"))
                    if (typeof message.rmssd !== "number")
                        return "rmssd: number expected";
                if (message.meanNn != null && message.hasOwnProperty("meanNn"))
                    if (typeof message.meanNn !== "number")
                        return "meanNn: number expected";
                if (message.sdnn != null && message.hasOwnProperty("sdnn"))
                    if (typeof message.sdnn !== "number")
                        return "sdnn: number expected";
                if (message.baevsky != null && message.hasOwnProperty("baevsky"))
                    if (typeof message.baevsky !== "number")
                        return "baevsky: number expected";
                if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                    if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                        return "timestamp: integer|Long expected";
                if (message.confidence != null && message.hasOwnProperty("confidence"))
                    if (typeof message.confidence !== "number")
                        return "confidence: number expected";
                return null;
            };

            /**
             * Creates a Hrv message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof presage.smartspectra.Hrv
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {presage.smartspectra.Hrv} Hrv
             */
            Hrv.fromObject = function fromObject(object, long) {
                if (object instanceof $root.presage.smartspectra.Hrv)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var message = new $root.presage.smartspectra.Hrv();
                if (object.rmssd != null)
                    message.rmssd = Number(object.rmssd);
                if (object.meanNn != null)
                    message.meanNn = Number(object.meanNn);
                if (object.sdnn != null)
                    message.sdnn = Number(object.sdnn);
                if (object.baevsky != null)
                    message.baevsky = Number(object.baevsky);
                if (object.timestamp != null)
                    if ($util.Long)
                        message.timestamp = $util.Long.fromValue(object.timestamp, false);
                    else if (typeof object.timestamp === "string")
                        message.timestamp = parseInt(object.timestamp, 10);
                    else if (typeof object.timestamp === "number")
                        message.timestamp = object.timestamp;
                    else if (typeof object.timestamp === "object")
                        message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
                if (object.confidence != null)
                    message.confidence = Number(object.confidence);
                return message;
            };

            /**
             * Creates a plain object from a Hrv message. Also converts values to other types if specified.
             * @function toObject
             * @memberof presage.smartspectra.Hrv
             * @static
             * @param {presage.smartspectra.Hrv} message Hrv
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Hrv.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.rmssd = 0;
                    object.meanNn = 0;
                    object.sdnn = 0;
                    object.baevsky = 0;
                    if ($util.Long) {
                        var long = new $util.Long(0, 0, false);
                        object.timestamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.timestamp = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    object.confidence = 0;
                }
                if (message.rmssd != null && message.hasOwnProperty("rmssd"))
                    object.rmssd = options.json && !isFinite(message.rmssd) ? String(message.rmssd) : message.rmssd;
                if (message.meanNn != null && message.hasOwnProperty("meanNn"))
                    object.meanNn = options.json && !isFinite(message.meanNn) ? String(message.meanNn) : message.meanNn;
                if (message.sdnn != null && message.hasOwnProperty("sdnn"))
                    object.sdnn = options.json && !isFinite(message.sdnn) ? String(message.sdnn) : message.sdnn;
                if (message.baevsky != null && message.hasOwnProperty("baevsky"))
                    object.baevsky = options.json && !isFinite(message.baevsky) ? String(message.baevsky) : message.baevsky;
                if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.timestamp === "number")
                        object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                    else
                        object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
                if (message.confidence != null && message.hasOwnProperty("confidence"))
                    object.confidence = options.json && !isFinite(message.confidence) ? String(message.confidence) : message.confidence;
                return object;
            };

            /**
             * Converts this Hrv to JSON.
             * @function toJSON
             * @memberof presage.smartspectra.Hrv
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Hrv.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Hrv
             * @function getTypeUrl
             * @memberof presage.smartspectra.Hrv
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Hrv.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/presage.smartspectra.Hrv";
            };

            return Hrv;
        })();

        smartspectra.Strict = (function() {

            /**
             * Properties of a Strict.
             * @memberof presage.smartspectra
             * @interface IStrict
             * @property {number|null} [value] The strict value requiring high precision
             */

            /**
             * Constructs a new Strict.
             * @memberof presage.smartspectra
             * @classdesc Container for strict/exact values that require high precision.
             * Used when measurements need to be treated with special precision requirements.
             * @implements IStrict
             * @constructor
             * @param {presage.smartspectra.IStrict=} [properties] Properties to set
             */
            function Strict(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * The strict value requiring high precision
             * @member {number} value
             * @memberof presage.smartspectra.Strict
             * @instance
             */
            Strict.prototype.value = 0;

            /**
             * Creates a new Strict instance using the specified properties.
             * @function create
             * @memberof presage.smartspectra.Strict
             * @static
             * @param {presage.smartspectra.IStrict=} [properties] Properties to set
             * @returns {presage.smartspectra.Strict} Strict instance
             */
            Strict.create = function create(properties) {
                return new Strict(properties);
            };

            /**
             * Encodes the specified Strict message. Does not implicitly {@link presage.smartspectra.Strict.verify|verify} messages.
             * @function encode
             * @memberof presage.smartspectra.Strict
             * @static
             * @param {presage.smartspectra.IStrict} message Strict message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Strict.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                    writer.uint32(/* id 1, wireType 5 =*/13).float(message.value);
                return writer;
            };

            /**
             * Encodes the specified Strict message, length delimited. Does not implicitly {@link presage.smartspectra.Strict.verify|verify} messages.
             * @function encodeDelimited
             * @memberof presage.smartspectra.Strict
             * @static
             * @param {presage.smartspectra.IStrict} message Strict message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Strict.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a Strict message from the specified reader or buffer.
             * @function decode
             * @memberof presage.smartspectra.Strict
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {presage.smartspectra.Strict} Strict
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Strict.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = new $root.presage.smartspectra.Strict();
                while (reader.pos < end) {
                    var tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.value = reader.float();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Strict message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof presage.smartspectra.Strict
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {presage.smartspectra.Strict} Strict
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Strict.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Strict message.
             * @function verify
             * @memberof presage.smartspectra.Strict
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Strict.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.value != null && message.hasOwnProperty("value"))
                    if (typeof message.value !== "number")
                        return "value: number expected";
                return null;
            };

            /**
             * Creates a Strict message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof presage.smartspectra.Strict
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {presage.smartspectra.Strict} Strict
             */
            Strict.fromObject = function fromObject(object, long) {
                if (object instanceof $root.presage.smartspectra.Strict)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var message = new $root.presage.smartspectra.Strict();
                if (object.value != null)
                    message.value = Number(object.value);
                return message;
            };

            /**
             * Creates a plain object from a Strict message. Also converts values to other types if specified.
             * @function toObject
             * @memberof presage.smartspectra.Strict
             * @static
             * @param {presage.smartspectra.Strict} message Strict
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Strict.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.value = 0;
                if (message.value != null && message.hasOwnProperty("value"))
                    object.value = options.json && !isFinite(message.value) ? String(message.value) : message.value;
                return object;
            };

            /**
             * Converts this Strict to JSON.
             * @function toJSON
             * @memberof presage.smartspectra.Strict
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Strict.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Strict
             * @function getTypeUrl
             * @memberof presage.smartspectra.Strict
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Strict.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/presage.smartspectra.Strict";
            };

            return Strict;
        })();

        smartspectra.Pulse = (function() {

            /**
             * Properties of a Pulse.
             * @memberof presage.smartspectra
             * @interface IPulse
             * @property {Array.<presage.smartspectra.IMeasurementWithConfidence>|null} [rate] Heart rate measurements with confidence scores
             * @property {Array.<presage.smartspectra.IMeasurement>|null} [trace] Raw pulse trace measurements
             * @property {Array.<presage.smartspectra.IMeasurement>|null} [pulseRespirationQuotient] Pulse-respiration quotient measurements indicating cardio-respiratory coupling
             * @property {presage.smartspectra.IStrict|null} [strict] Strict/high-precision pulse measurements over a fixed time interval.
             * Populated when strict mode analysis is enabled.
             */

            /**
             * Constructs a new Pulse.
             * @memberof presage.smartspectra
             * @classdesc Comprehensive pulse-related measurements and derived metrics.
             * Contains heart rate, pulse trace, and respiratory coupling information.
             * @implements IPulse
             * @constructor
             * @param {presage.smartspectra.IPulse=} [properties] Properties to set
             */
            function Pulse(properties) {
                this.rate = [];
                this.trace = [];
                this.pulseRespirationQuotient = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Heart rate measurements with confidence scores
             * @member {Array.<presage.smartspectra.IMeasurementWithConfidence>} rate
             * @memberof presage.smartspectra.Pulse
             * @instance
             */
            Pulse.prototype.rate = $util.emptyArray;

            /**
             * Raw pulse trace measurements
             * @member {Array.<presage.smartspectra.IMeasurement>} trace
             * @memberof presage.smartspectra.Pulse
             * @instance
             */
            Pulse.prototype.trace = $util.emptyArray;

            /**
             * Pulse-respiration quotient measurements indicating cardio-respiratory coupling
             * @member {Array.<presage.smartspectra.IMeasurement>} pulseRespirationQuotient
             * @memberof presage.smartspectra.Pulse
             * @instance
             */
            Pulse.prototype.pulseRespirationQuotient = $util.emptyArray;

            /**
             * Strict/high-precision pulse measurements over a fixed time interval.
             * Populated when strict mode analysis is enabled.
             * @member {presage.smartspectra.IStrict|null|undefined} strict
             * @memberof presage.smartspectra.Pulse
             * @instance
             */
            Pulse.prototype.strict = null;

            /**
             * Creates a new Pulse instance using the specified properties.
             * @function create
             * @memberof presage.smartspectra.Pulse
             * @static
             * @param {presage.smartspectra.IPulse=} [properties] Properties to set
             * @returns {presage.smartspectra.Pulse} Pulse instance
             */
            Pulse.create = function create(properties) {
                return new Pulse(properties);
            };

            /**
             * Encodes the specified Pulse message. Does not implicitly {@link presage.smartspectra.Pulse.verify|verify} messages.
             * @function encode
             * @memberof presage.smartspectra.Pulse
             * @static
             * @param {presage.smartspectra.IPulse} message Pulse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Pulse.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.rate != null && message.rate.length)
                    for (var i = 0; i < message.rate.length; ++i)
                        $root.presage.smartspectra.MeasurementWithConfidence.encode(message.rate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.trace != null && message.trace.length)
                    for (var i = 0; i < message.trace.length; ++i)
                        $root.presage.smartspectra.Measurement.encode(message.trace[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.pulseRespirationQuotient != null && message.pulseRespirationQuotient.length)
                    for (var i = 0; i < message.pulseRespirationQuotient.length; ++i)
                        $root.presage.smartspectra.Measurement.encode(message.pulseRespirationQuotient[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.strict != null && Object.hasOwnProperty.call(message, "strict"))
                    $root.presage.smartspectra.Strict.encode(message.strict, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified Pulse message, length delimited. Does not implicitly {@link presage.smartspectra.Pulse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof presage.smartspectra.Pulse
             * @static
             * @param {presage.smartspectra.IPulse} message Pulse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Pulse.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a Pulse message from the specified reader or buffer.
             * @function decode
             * @memberof presage.smartspectra.Pulse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {presage.smartspectra.Pulse} Pulse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Pulse.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = new $root.presage.smartspectra.Pulse();
                while (reader.pos < end) {
                    var tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            if (!(message.rate && message.rate.length))
                                message.rate = [];
                            message.rate.push($root.presage.smartspectra.MeasurementWithConfidence.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 2: {
                            if (!(message.trace && message.trace.length))
                                message.trace = [];
                            message.trace.push($root.presage.smartspectra.Measurement.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 3: {
                            if (!(message.pulseRespirationQuotient && message.pulseRespirationQuotient.length))
                                message.pulseRespirationQuotient = [];
                            message.pulseRespirationQuotient.push($root.presage.smartspectra.Measurement.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 4: {
                            message.strict = $root.presage.smartspectra.Strict.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Pulse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof presage.smartspectra.Pulse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {presage.smartspectra.Pulse} Pulse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Pulse.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Pulse message.
             * @function verify
             * @memberof presage.smartspectra.Pulse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Pulse.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.rate != null && message.hasOwnProperty("rate")) {
                    if (!Array.isArray(message.rate))
                        return "rate: array expected";
                    for (var i = 0; i < message.rate.length; ++i) {
                        var error = $root.presage.smartspectra.MeasurementWithConfidence.verify(message.rate[i], long + 1);
                        if (error)
                            return "rate." + error;
                    }
                }
                if (message.trace != null && message.hasOwnProperty("trace")) {
                    if (!Array.isArray(message.trace))
                        return "trace: array expected";
                    for (var i = 0; i < message.trace.length; ++i) {
                        var error = $root.presage.smartspectra.Measurement.verify(message.trace[i], long + 1);
                        if (error)
                            return "trace." + error;
                    }
                }
                if (message.pulseRespirationQuotient != null && message.hasOwnProperty("pulseRespirationQuotient")) {
                    if (!Array.isArray(message.pulseRespirationQuotient))
                        return "pulseRespirationQuotient: array expected";
                    for (var i = 0; i < message.pulseRespirationQuotient.length; ++i) {
                        var error = $root.presage.smartspectra.Measurement.verify(message.pulseRespirationQuotient[i], long + 1);
                        if (error)
                            return "pulseRespirationQuotient." + error;
                    }
                }
                if (message.strict != null && message.hasOwnProperty("strict")) {
                    var error = $root.presage.smartspectra.Strict.verify(message.strict, long + 1);
                    if (error)
                        return "strict." + error;
                }
                return null;
            };

            /**
             * Creates a Pulse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof presage.smartspectra.Pulse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {presage.smartspectra.Pulse} Pulse
             */
            Pulse.fromObject = function fromObject(object, long) {
                if (object instanceof $root.presage.smartspectra.Pulse)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var message = new $root.presage.smartspectra.Pulse();
                if (object.rate) {
                    if (!Array.isArray(object.rate))
                        throw TypeError(".presage.smartspectra.Pulse.rate: array expected");
                    message.rate = [];
                    for (var i = 0; i < object.rate.length; ++i) {
                        if (typeof object.rate[i] !== "object")
                            throw TypeError(".presage.smartspectra.Pulse.rate: object expected");
                        message.rate[i] = $root.presage.smartspectra.MeasurementWithConfidence.fromObject(object.rate[i], long + 1);
                    }
                }
                if (object.trace) {
                    if (!Array.isArray(object.trace))
                        throw TypeError(".presage.smartspectra.Pulse.trace: array expected");
                    message.trace = [];
                    for (var i = 0; i < object.trace.length; ++i) {
                        if (typeof object.trace[i] !== "object")
                            throw TypeError(".presage.smartspectra.Pulse.trace: object expected");
                        message.trace[i] = $root.presage.smartspectra.Measurement.fromObject(object.trace[i], long + 1);
                    }
                }
                if (object.pulseRespirationQuotient) {
                    if (!Array.isArray(object.pulseRespirationQuotient))
                        throw TypeError(".presage.smartspectra.Pulse.pulseRespirationQuotient: array expected");
                    message.pulseRespirationQuotient = [];
                    for (var i = 0; i < object.pulseRespirationQuotient.length; ++i) {
                        if (typeof object.pulseRespirationQuotient[i] !== "object")
                            throw TypeError(".presage.smartspectra.Pulse.pulseRespirationQuotient: object expected");
                        message.pulseRespirationQuotient[i] = $root.presage.smartspectra.Measurement.fromObject(object.pulseRespirationQuotient[i], long + 1);
                    }
                }
                if (object.strict != null) {
                    if (typeof object.strict !== "object")
                        throw TypeError(".presage.smartspectra.Pulse.strict: object expected");
                    message.strict = $root.presage.smartspectra.Strict.fromObject(object.strict, long + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a Pulse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof presage.smartspectra.Pulse
             * @static
             * @param {presage.smartspectra.Pulse} message Pulse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Pulse.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.rate = [];
                    object.trace = [];
                    object.pulseRespirationQuotient = [];
                }
                if (options.defaults)
                    object.strict = null;
                if (message.rate && message.rate.length) {
                    object.rate = [];
                    for (var j = 0; j < message.rate.length; ++j)
                        object.rate[j] = $root.presage.smartspectra.MeasurementWithConfidence.toObject(message.rate[j], options, q + 1);
                }
                if (message.trace && message.trace.length) {
                    object.trace = [];
                    for (var j = 0; j < message.trace.length; ++j)
                        object.trace[j] = $root.presage.smartspectra.Measurement.toObject(message.trace[j], options, q + 1);
                }
                if (message.pulseRespirationQuotient && message.pulseRespirationQuotient.length) {
                    object.pulseRespirationQuotient = [];
                    for (var j = 0; j < message.pulseRespirationQuotient.length; ++j)
                        object.pulseRespirationQuotient[j] = $root.presage.smartspectra.Measurement.toObject(message.pulseRespirationQuotient[j], options, q + 1);
                }
                if (message.strict != null && message.hasOwnProperty("strict"))
                    object.strict = $root.presage.smartspectra.Strict.toObject(message.strict, options, q + 1);
                return object;
            };

            /**
             * Converts this Pulse to JSON.
             * @function toJSON
             * @memberof presage.smartspectra.Pulse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Pulse.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Pulse
             * @function getTypeUrl
             * @memberof presage.smartspectra.Pulse
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Pulse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/presage.smartspectra.Pulse";
            };

            return Pulse;
        })();

        smartspectra.Breathing = (function() {

            /**
             * Properties of a Breathing.
             * @memberof presage.smartspectra
             * @interface IBreathing
             * @property {Array.<presage.smartspectra.IMeasurementWithConfidence>|null} [rate] Respiratory rate measurements with confidence scores
             * @property {Array.<presage.smartspectra.IMeasurement>|null} [upperTrace] Chest breathing movement trace measurements
             * @property {Array.<presage.smartspectra.IMeasurement>|null} [lowerTrace] Abdominal breathing movement trace measurements
             * @property {Array.<presage.smartspectra.IMeasurement>|null} [amplitude] Breathing amplitude measurements
             * @property {Array.<presage.smartspectra.IDetectionStatus>|null} [apnea] Apnea (breathing cessation) detection status
             * @property {Array.<presage.smartspectra.IMeasurement>|null} [respiratoryLineLength] Respiratory line length measurements for breathing pattern analysis
             * @property {Array.<presage.smartspectra.IMeasurement>|null} [baseline] Baseline breathing measurements
             * @property {Array.<presage.smartspectra.IMeasurement>|null} [inhaleExhaleRatio] Inhale to exhale duration ratio measurements
             * @property {presage.smartspectra.IStrict|null} [strict] Strict/high-precision breathing measurements over a fixed time interval.
             * Populated when strict mode analysis is enabled.
             */

            /**
             * Constructs a new Breathing.
             * @memberof presage.smartspectra
             * @classdesc Comprehensive breathing/respiratory measurements and derived metrics.
             * Contains respiratory rate, traces, and various breathing pattern indicators.
             * @implements IBreathing
             * @constructor
             * @param {presage.smartspectra.IBreathing=} [properties] Properties to set
             */
            function Breathing(properties) {
                this.rate = [];
                this.upperTrace = [];
                this.lowerTrace = [];
                this.amplitude = [];
                this.apnea = [];
                this.respiratoryLineLength = [];
                this.baseline = [];
                this.inhaleExhaleRatio = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Respiratory rate measurements with confidence scores
             * @member {Array.<presage.smartspectra.IMeasurementWithConfidence>} rate
             * @memberof presage.smartspectra.Breathing
             * @instance
             */
            Breathing.prototype.rate = $util.emptyArray;

            /**
             * Chest breathing movement trace measurements
             * @member {Array.<presage.smartspectra.IMeasurement>} upperTrace
             * @memberof presage.smartspectra.Breathing
             * @instance
             */
            Breathing.prototype.upperTrace = $util.emptyArray;

            /**
             * Abdominal breathing movement trace measurements
             * @member {Array.<presage.smartspectra.IMeasurement>} lowerTrace
             * @memberof presage.smartspectra.Breathing
             * @instance
             */
            Breathing.prototype.lowerTrace = $util.emptyArray;

            /**
             * Breathing amplitude measurements
             * @member {Array.<presage.smartspectra.IMeasurement>} amplitude
             * @memberof presage.smartspectra.Breathing
             * @instance
             */
            Breathing.prototype.amplitude = $util.emptyArray;

            /**
             * Apnea (breathing cessation) detection status
             * @member {Array.<presage.smartspectra.IDetectionStatus>} apnea
             * @memberof presage.smartspectra.Breathing
             * @instance
             */
            Breathing.prototype.apnea = $util.emptyArray;

            /**
             * Respiratory line length measurements for breathing pattern analysis
             * @member {Array.<presage.smartspectra.IMeasurement>} respiratoryLineLength
             * @memberof presage.smartspectra.Breathing
             * @instance
             */
            Breathing.prototype.respiratoryLineLength = $util.emptyArray;

            /**
             * Baseline breathing measurements
             * @member {Array.<presage.smartspectra.IMeasurement>} baseline
             * @memberof presage.smartspectra.Breathing
             * @instance
             */
            Breathing.prototype.baseline = $util.emptyArray;

            /**
             * Inhale to exhale duration ratio measurements
             * @member {Array.<presage.smartspectra.IMeasurement>} inhaleExhaleRatio
             * @memberof presage.smartspectra.Breathing
             * @instance
             */
            Breathing.prototype.inhaleExhaleRatio = $util.emptyArray;

            /**
             * Strict/high-precision breathing measurements over a fixed time interval.
             * Populated when strict mode analysis is enabled.
             * @member {presage.smartspectra.IStrict|null|undefined} strict
             * @memberof presage.smartspectra.Breathing
             * @instance
             */
            Breathing.prototype.strict = null;

            /**
             * Creates a new Breathing instance using the specified properties.
             * @function create
             * @memberof presage.smartspectra.Breathing
             * @static
             * @param {presage.smartspectra.IBreathing=} [properties] Properties to set
             * @returns {presage.smartspectra.Breathing} Breathing instance
             */
            Breathing.create = function create(properties) {
                return new Breathing(properties);
            };

            /**
             * Encodes the specified Breathing message. Does not implicitly {@link presage.smartspectra.Breathing.verify|verify} messages.
             * @function encode
             * @memberof presage.smartspectra.Breathing
             * @static
             * @param {presage.smartspectra.IBreathing} message Breathing message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Breathing.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.rate != null && message.rate.length)
                    for (var i = 0; i < message.rate.length; ++i)
                        $root.presage.smartspectra.MeasurementWithConfidence.encode(message.rate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.upperTrace != null && message.upperTrace.length)
                    for (var i = 0; i < message.upperTrace.length; ++i)
                        $root.presage.smartspectra.Measurement.encode(message.upperTrace[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.lowerTrace != null && message.lowerTrace.length)
                    for (var i = 0; i < message.lowerTrace.length; ++i)
                        $root.presage.smartspectra.Measurement.encode(message.lowerTrace[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.amplitude != null && message.amplitude.length)
                    for (var i = 0; i < message.amplitude.length; ++i)
                        $root.presage.smartspectra.Measurement.encode(message.amplitude[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.apnea != null && message.apnea.length)
                    for (var i = 0; i < message.apnea.length; ++i)
                        $root.presage.smartspectra.DetectionStatus.encode(message.apnea[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                if (message.respiratoryLineLength != null && message.respiratoryLineLength.length)
                    for (var i = 0; i < message.respiratoryLineLength.length; ++i)
                        $root.presage.smartspectra.Measurement.encode(message.respiratoryLineLength[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
                if (message.baseline != null && message.baseline.length)
                    for (var i = 0; i < message.baseline.length; ++i)
                        $root.presage.smartspectra.Measurement.encode(message.baseline[i], writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
                if (message.inhaleExhaleRatio != null && message.inhaleExhaleRatio.length)
                    for (var i = 0; i < message.inhaleExhaleRatio.length; ++i)
                        $root.presage.smartspectra.Measurement.encode(message.inhaleExhaleRatio[i], writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
                if (message.strict != null && Object.hasOwnProperty.call(message, "strict"))
                    $root.presage.smartspectra.Strict.encode(message.strict, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified Breathing message, length delimited. Does not implicitly {@link presage.smartspectra.Breathing.verify|verify} messages.
             * @function encodeDelimited
             * @memberof presage.smartspectra.Breathing
             * @static
             * @param {presage.smartspectra.IBreathing} message Breathing message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Breathing.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a Breathing message from the specified reader or buffer.
             * @function decode
             * @memberof presage.smartspectra.Breathing
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {presage.smartspectra.Breathing} Breathing
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Breathing.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = new $root.presage.smartspectra.Breathing();
                while (reader.pos < end) {
                    var tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            if (!(message.rate && message.rate.length))
                                message.rate = [];
                            message.rate.push($root.presage.smartspectra.MeasurementWithConfidence.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 2: {
                            if (!(message.upperTrace && message.upperTrace.length))
                                message.upperTrace = [];
                            message.upperTrace.push($root.presage.smartspectra.Measurement.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 3: {
                            if (!(message.lowerTrace && message.lowerTrace.length))
                                message.lowerTrace = [];
                            message.lowerTrace.push($root.presage.smartspectra.Measurement.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 4: {
                            if (!(message.amplitude && message.amplitude.length))
                                message.amplitude = [];
                            message.amplitude.push($root.presage.smartspectra.Measurement.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 5: {
                            if (!(message.apnea && message.apnea.length))
                                message.apnea = [];
                            message.apnea.push($root.presage.smartspectra.DetectionStatus.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 6: {
                            if (!(message.respiratoryLineLength && message.respiratoryLineLength.length))
                                message.respiratoryLineLength = [];
                            message.respiratoryLineLength.push($root.presage.smartspectra.Measurement.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 7: {
                            if (!(message.baseline && message.baseline.length))
                                message.baseline = [];
                            message.baseline.push($root.presage.smartspectra.Measurement.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 8: {
                            if (!(message.inhaleExhaleRatio && message.inhaleExhaleRatio.length))
                                message.inhaleExhaleRatio = [];
                            message.inhaleExhaleRatio.push($root.presage.smartspectra.Measurement.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 9: {
                            message.strict = $root.presage.smartspectra.Strict.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Breathing message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof presage.smartspectra.Breathing
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {presage.smartspectra.Breathing} Breathing
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Breathing.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Breathing message.
             * @function verify
             * @memberof presage.smartspectra.Breathing
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Breathing.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.rate != null && message.hasOwnProperty("rate")) {
                    if (!Array.isArray(message.rate))
                        return "rate: array expected";
                    for (var i = 0; i < message.rate.length; ++i) {
                        var error = $root.presage.smartspectra.MeasurementWithConfidence.verify(message.rate[i], long + 1);
                        if (error)
                            return "rate." + error;
                    }
                }
                if (message.upperTrace != null && message.hasOwnProperty("upperTrace")) {
                    if (!Array.isArray(message.upperTrace))
                        return "upperTrace: array expected";
                    for (var i = 0; i < message.upperTrace.length; ++i) {
                        var error = $root.presage.smartspectra.Measurement.verify(message.upperTrace[i], long + 1);
                        if (error)
                            return "upperTrace." + error;
                    }
                }
                if (message.lowerTrace != null && message.hasOwnProperty("lowerTrace")) {
                    if (!Array.isArray(message.lowerTrace))
                        return "lowerTrace: array expected";
                    for (var i = 0; i < message.lowerTrace.length; ++i) {
                        var error = $root.presage.smartspectra.Measurement.verify(message.lowerTrace[i], long + 1);
                        if (error)
                            return "lowerTrace." + error;
                    }
                }
                if (message.amplitude != null && message.hasOwnProperty("amplitude")) {
                    if (!Array.isArray(message.amplitude))
                        return "amplitude: array expected";
                    for (var i = 0; i < message.amplitude.length; ++i) {
                        var error = $root.presage.smartspectra.Measurement.verify(message.amplitude[i], long + 1);
                        if (error)
                            return "amplitude." + error;
                    }
                }
                if (message.apnea != null && message.hasOwnProperty("apnea")) {
                    if (!Array.isArray(message.apnea))
                        return "apnea: array expected";
                    for (var i = 0; i < message.apnea.length; ++i) {
                        var error = $root.presage.smartspectra.DetectionStatus.verify(message.apnea[i], long + 1);
                        if (error)
                            return "apnea." + error;
                    }
                }
                if (message.respiratoryLineLength != null && message.hasOwnProperty("respiratoryLineLength")) {
                    if (!Array.isArray(message.respiratoryLineLength))
                        return "respiratoryLineLength: array expected";
                    for (var i = 0; i < message.respiratoryLineLength.length; ++i) {
                        var error = $root.presage.smartspectra.Measurement.verify(message.respiratoryLineLength[i], long + 1);
                        if (error)
                            return "respiratoryLineLength." + error;
                    }
                }
                if (message.baseline != null && message.hasOwnProperty("baseline")) {
                    if (!Array.isArray(message.baseline))
                        return "baseline: array expected";
                    for (var i = 0; i < message.baseline.length; ++i) {
                        var error = $root.presage.smartspectra.Measurement.verify(message.baseline[i], long + 1);
                        if (error)
                            return "baseline." + error;
                    }
                }
                if (message.inhaleExhaleRatio != null && message.hasOwnProperty("inhaleExhaleRatio")) {
                    if (!Array.isArray(message.inhaleExhaleRatio))
                        return "inhaleExhaleRatio: array expected";
                    for (var i = 0; i < message.inhaleExhaleRatio.length; ++i) {
                        var error = $root.presage.smartspectra.Measurement.verify(message.inhaleExhaleRatio[i], long + 1);
                        if (error)
                            return "inhaleExhaleRatio." + error;
                    }
                }
                if (message.strict != null && message.hasOwnProperty("strict")) {
                    var error = $root.presage.smartspectra.Strict.verify(message.strict, long + 1);
                    if (error)
                        return "strict." + error;
                }
                return null;
            };

            /**
             * Creates a Breathing message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof presage.smartspectra.Breathing
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {presage.smartspectra.Breathing} Breathing
             */
            Breathing.fromObject = function fromObject(object, long) {
                if (object instanceof $root.presage.smartspectra.Breathing)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var message = new $root.presage.smartspectra.Breathing();
                if (object.rate) {
                    if (!Array.isArray(object.rate))
                        throw TypeError(".presage.smartspectra.Breathing.rate: array expected");
                    message.rate = [];
                    for (var i = 0; i < object.rate.length; ++i) {
                        if (typeof object.rate[i] !== "object")
                            throw TypeError(".presage.smartspectra.Breathing.rate: object expected");
                        message.rate[i] = $root.presage.smartspectra.MeasurementWithConfidence.fromObject(object.rate[i], long + 1);
                    }
                }
                if (object.upperTrace) {
                    if (!Array.isArray(object.upperTrace))
                        throw TypeError(".presage.smartspectra.Breathing.upperTrace: array expected");
                    message.upperTrace = [];
                    for (var i = 0; i < object.upperTrace.length; ++i) {
                        if (typeof object.upperTrace[i] !== "object")
                            throw TypeError(".presage.smartspectra.Breathing.upperTrace: object expected");
                        message.upperTrace[i] = $root.presage.smartspectra.Measurement.fromObject(object.upperTrace[i], long + 1);
                    }
                }
                if (object.lowerTrace) {
                    if (!Array.isArray(object.lowerTrace))
                        throw TypeError(".presage.smartspectra.Breathing.lowerTrace: array expected");
                    message.lowerTrace = [];
                    for (var i = 0; i < object.lowerTrace.length; ++i) {
                        if (typeof object.lowerTrace[i] !== "object")
                            throw TypeError(".presage.smartspectra.Breathing.lowerTrace: object expected");
                        message.lowerTrace[i] = $root.presage.smartspectra.Measurement.fromObject(object.lowerTrace[i], long + 1);
                    }
                }
                if (object.amplitude) {
                    if (!Array.isArray(object.amplitude))
                        throw TypeError(".presage.smartspectra.Breathing.amplitude: array expected");
                    message.amplitude = [];
                    for (var i = 0; i < object.amplitude.length; ++i) {
                        if (typeof object.amplitude[i] !== "object")
                            throw TypeError(".presage.smartspectra.Breathing.amplitude: object expected");
                        message.amplitude[i] = $root.presage.smartspectra.Measurement.fromObject(object.amplitude[i], long + 1);
                    }
                }
                if (object.apnea) {
                    if (!Array.isArray(object.apnea))
                        throw TypeError(".presage.smartspectra.Breathing.apnea: array expected");
                    message.apnea = [];
                    for (var i = 0; i < object.apnea.length; ++i) {
                        if (typeof object.apnea[i] !== "object")
                            throw TypeError(".presage.smartspectra.Breathing.apnea: object expected");
                        message.apnea[i] = $root.presage.smartspectra.DetectionStatus.fromObject(object.apnea[i], long + 1);
                    }
                }
                if (object.respiratoryLineLength) {
                    if (!Array.isArray(object.respiratoryLineLength))
                        throw TypeError(".presage.smartspectra.Breathing.respiratoryLineLength: array expected");
                    message.respiratoryLineLength = [];
                    for (var i = 0; i < object.respiratoryLineLength.length; ++i) {
                        if (typeof object.respiratoryLineLength[i] !== "object")
                            throw TypeError(".presage.smartspectra.Breathing.respiratoryLineLength: object expected");
                        message.respiratoryLineLength[i] = $root.presage.smartspectra.Measurement.fromObject(object.respiratoryLineLength[i], long + 1);
                    }
                }
                if (object.baseline) {
                    if (!Array.isArray(object.baseline))
                        throw TypeError(".presage.smartspectra.Breathing.baseline: array expected");
                    message.baseline = [];
                    for (var i = 0; i < object.baseline.length; ++i) {
                        if (typeof object.baseline[i] !== "object")
                            throw TypeError(".presage.smartspectra.Breathing.baseline: object expected");
                        message.baseline[i] = $root.presage.smartspectra.Measurement.fromObject(object.baseline[i], long + 1);
                    }
                }
                if (object.inhaleExhaleRatio) {
                    if (!Array.isArray(object.inhaleExhaleRatio))
                        throw TypeError(".presage.smartspectra.Breathing.inhaleExhaleRatio: array expected");
                    message.inhaleExhaleRatio = [];
                    for (var i = 0; i < object.inhaleExhaleRatio.length; ++i) {
                        if (typeof object.inhaleExhaleRatio[i] !== "object")
                            throw TypeError(".presage.smartspectra.Breathing.inhaleExhaleRatio: object expected");
                        message.inhaleExhaleRatio[i] = $root.presage.smartspectra.Measurement.fromObject(object.inhaleExhaleRatio[i], long + 1);
                    }
                }
                if (object.strict != null) {
                    if (typeof object.strict !== "object")
                        throw TypeError(".presage.smartspectra.Breathing.strict: object expected");
                    message.strict = $root.presage.smartspectra.Strict.fromObject(object.strict, long + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a Breathing message. Also converts values to other types if specified.
             * @function toObject
             * @memberof presage.smartspectra.Breathing
             * @static
             * @param {presage.smartspectra.Breathing} message Breathing
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Breathing.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.rate = [];
                    object.upperTrace = [];
                    object.lowerTrace = [];
                    object.amplitude = [];
                    object.apnea = [];
                    object.respiratoryLineLength = [];
                    object.baseline = [];
                    object.inhaleExhaleRatio = [];
                }
                if (options.defaults)
                    object.strict = null;
                if (message.rate && message.rate.length) {
                    object.rate = [];
                    for (var j = 0; j < message.rate.length; ++j)
                        object.rate[j] = $root.presage.smartspectra.MeasurementWithConfidence.toObject(message.rate[j], options, q + 1);
                }
                if (message.upperTrace && message.upperTrace.length) {
                    object.upperTrace = [];
                    for (var j = 0; j < message.upperTrace.length; ++j)
                        object.upperTrace[j] = $root.presage.smartspectra.Measurement.toObject(message.upperTrace[j], options, q + 1);
                }
                if (message.lowerTrace && message.lowerTrace.length) {
                    object.lowerTrace = [];
                    for (var j = 0; j < message.lowerTrace.length; ++j)
                        object.lowerTrace[j] = $root.presage.smartspectra.Measurement.toObject(message.lowerTrace[j], options, q + 1);
                }
                if (message.amplitude && message.amplitude.length) {
                    object.amplitude = [];
                    for (var j = 0; j < message.amplitude.length; ++j)
                        object.amplitude[j] = $root.presage.smartspectra.Measurement.toObject(message.amplitude[j], options, q + 1);
                }
                if (message.apnea && message.apnea.length) {
                    object.apnea = [];
                    for (var j = 0; j < message.apnea.length; ++j)
                        object.apnea[j] = $root.presage.smartspectra.DetectionStatus.toObject(message.apnea[j], options, q + 1);
                }
                if (message.respiratoryLineLength && message.respiratoryLineLength.length) {
                    object.respiratoryLineLength = [];
                    for (var j = 0; j < message.respiratoryLineLength.length; ++j)
                        object.respiratoryLineLength[j] = $root.presage.smartspectra.Measurement.toObject(message.respiratoryLineLength[j], options, q + 1);
                }
                if (message.baseline && message.baseline.length) {
                    object.baseline = [];
                    for (var j = 0; j < message.baseline.length; ++j)
                        object.baseline[j] = $root.presage.smartspectra.Measurement.toObject(message.baseline[j], options, q + 1);
                }
                if (message.inhaleExhaleRatio && message.inhaleExhaleRatio.length) {
                    object.inhaleExhaleRatio = [];
                    for (var j = 0; j < message.inhaleExhaleRatio.length; ++j)
                        object.inhaleExhaleRatio[j] = $root.presage.smartspectra.Measurement.toObject(message.inhaleExhaleRatio[j], options, q + 1);
                }
                if (message.strict != null && message.hasOwnProperty("strict"))
                    object.strict = $root.presage.smartspectra.Strict.toObject(message.strict, options, q + 1);
                return object;
            };

            /**
             * Converts this Breathing to JSON.
             * @function toJSON
             * @memberof presage.smartspectra.Breathing
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Breathing.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Breathing
             * @function getTypeUrl
             * @memberof presage.smartspectra.Breathing
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Breathing.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/presage.smartspectra.Breathing";
            };

            return Breathing;
        })();

        smartspectra.Landmarks = (function() {

            /**
             * Properties of a Landmarks.
             * @memberof presage.smartspectra
             * @interface ILandmarks
             * @property {Array.<presage.smartspectra.IPoint2dFloat>|null} [value] Array of 2D coordinate points representing facial landmarks
             * @property {boolean|null} [stable] Whether the landmark detection is considered stable/reliable
             * @property {boolean|null} [reset] Indicates whether the landmark set was reset (cannot be directly associated with previous set)
             * @property {number|Long|null} [timestamp] Absolute timestamp at which the landmarks were detected, in microseconds, since Linux epoch
             */

            /**
             * Constructs a new Landmarks.
             * @memberof presage.smartspectra
             * @classdesc Facial landmark coordinates with temporal and stability information.
             * Used for face tracking and facial feature analysis.
             * @implements ILandmarks
             * @constructor
             * @param {presage.smartspectra.ILandmarks=} [properties] Properties to set
             */
            function Landmarks(properties) {
                this.value = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Array of 2D coordinate points representing facial landmarks
             * @member {Array.<presage.smartspectra.IPoint2dFloat>} value
             * @memberof presage.smartspectra.Landmarks
             * @instance
             */
            Landmarks.prototype.value = $util.emptyArray;

            /**
             * Whether the landmark detection is considered stable/reliable
             * @member {boolean} stable
             * @memberof presage.smartspectra.Landmarks
             * @instance
             */
            Landmarks.prototype.stable = false;

            /**
             * Indicates whether the landmark set was reset (cannot be directly associated with previous set)
             * @member {boolean} reset
             * @memberof presage.smartspectra.Landmarks
             * @instance
             */
            Landmarks.prototype.reset = false;

            /**
             * Absolute timestamp at which the landmarks were detected, in microseconds, since Linux epoch
             * @member {number|Long} timestamp
             * @memberof presage.smartspectra.Landmarks
             * @instance
             */
            Landmarks.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * Creates a new Landmarks instance using the specified properties.
             * @function create
             * @memberof presage.smartspectra.Landmarks
             * @static
             * @param {presage.smartspectra.ILandmarks=} [properties] Properties to set
             * @returns {presage.smartspectra.Landmarks} Landmarks instance
             */
            Landmarks.create = function create(properties) {
                return new Landmarks(properties);
            };

            /**
             * Encodes the specified Landmarks message. Does not implicitly {@link presage.smartspectra.Landmarks.verify|verify} messages.
             * @function encode
             * @memberof presage.smartspectra.Landmarks
             * @static
             * @param {presage.smartspectra.ILandmarks} message Landmarks message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Landmarks.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.value != null && message.value.length)
                    for (var i = 0; i < message.value.length; ++i)
                        $root.presage.smartspectra.Point2dFloat.encode(message.value[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.stable != null && Object.hasOwnProperty.call(message, "stable"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.stable);
                if (message.reset != null && Object.hasOwnProperty.call(message, "reset"))
                    writer.uint32(/* id 3, wireType 0 =*/24).bool(message.reset);
                if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int64(message.timestamp);
                return writer;
            };

            /**
             * Encodes the specified Landmarks message, length delimited. Does not implicitly {@link presage.smartspectra.Landmarks.verify|verify} messages.
             * @function encodeDelimited
             * @memberof presage.smartspectra.Landmarks
             * @static
             * @param {presage.smartspectra.ILandmarks} message Landmarks message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Landmarks.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a Landmarks message from the specified reader or buffer.
             * @function decode
             * @memberof presage.smartspectra.Landmarks
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {presage.smartspectra.Landmarks} Landmarks
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Landmarks.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = new $root.presage.smartspectra.Landmarks();
                while (reader.pos < end) {
                    var tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            if (!(message.value && message.value.length))
                                message.value = [];
                            message.value.push($root.presage.smartspectra.Point2dFloat.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 2: {
                            message.stable = reader.bool();
                            break;
                        }
                    case 3: {
                            message.reset = reader.bool();
                            break;
                        }
                    case 4: {
                            message.timestamp = reader.int64();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Landmarks message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof presage.smartspectra.Landmarks
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {presage.smartspectra.Landmarks} Landmarks
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Landmarks.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Landmarks message.
             * @function verify
             * @memberof presage.smartspectra.Landmarks
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Landmarks.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.value != null && message.hasOwnProperty("value")) {
                    if (!Array.isArray(message.value))
                        return "value: array expected";
                    for (var i = 0; i < message.value.length; ++i) {
                        var error = $root.presage.smartspectra.Point2dFloat.verify(message.value[i], long + 1);
                        if (error)
                            return "value." + error;
                    }
                }
                if (message.stable != null && message.hasOwnProperty("stable"))
                    if (typeof message.stable !== "boolean")
                        return "stable: boolean expected";
                if (message.reset != null && message.hasOwnProperty("reset"))
                    if (typeof message.reset !== "boolean")
                        return "reset: boolean expected";
                if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                    if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                        return "timestamp: integer|Long expected";
                return null;
            };

            /**
             * Creates a Landmarks message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof presage.smartspectra.Landmarks
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {presage.smartspectra.Landmarks} Landmarks
             */
            Landmarks.fromObject = function fromObject(object, long) {
                if (object instanceof $root.presage.smartspectra.Landmarks)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var message = new $root.presage.smartspectra.Landmarks();
                if (object.value) {
                    if (!Array.isArray(object.value))
                        throw TypeError(".presage.smartspectra.Landmarks.value: array expected");
                    message.value = [];
                    for (var i = 0; i < object.value.length; ++i) {
                        if (typeof object.value[i] !== "object")
                            throw TypeError(".presage.smartspectra.Landmarks.value: object expected");
                        message.value[i] = $root.presage.smartspectra.Point2dFloat.fromObject(object.value[i], long + 1);
                    }
                }
                if (object.stable != null)
                    message.stable = Boolean(object.stable);
                if (object.reset != null)
                    message.reset = Boolean(object.reset);
                if (object.timestamp != null)
                    if ($util.Long)
                        message.timestamp = $util.Long.fromValue(object.timestamp, false);
                    else if (typeof object.timestamp === "string")
                        message.timestamp = parseInt(object.timestamp, 10);
                    else if (typeof object.timestamp === "number")
                        message.timestamp = object.timestamp;
                    else if (typeof object.timestamp === "object")
                        message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a Landmarks message. Also converts values to other types if specified.
             * @function toObject
             * @memberof presage.smartspectra.Landmarks
             * @static
             * @param {presage.smartspectra.Landmarks} message Landmarks
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Landmarks.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.value = [];
                if (options.defaults) {
                    object.stable = false;
                    object.reset = false;
                    if ($util.Long) {
                        var long = new $util.Long(0, 0, false);
                        object.timestamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.timestamp = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                }
                if (message.value && message.value.length) {
                    object.value = [];
                    for (var j = 0; j < message.value.length; ++j)
                        object.value[j] = $root.presage.smartspectra.Point2dFloat.toObject(message.value[j], options, q + 1);
                }
                if (message.stable != null && message.hasOwnProperty("stable"))
                    object.stable = message.stable;
                if (message.reset != null && message.hasOwnProperty("reset"))
                    object.reset = message.reset;
                if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.timestamp === "number")
                        object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                    else
                        object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
                return object;
            };

            /**
             * Converts this Landmarks to JSON.
             * @function toJSON
             * @memberof presage.smartspectra.Landmarks
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Landmarks.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Landmarks
             * @function getTypeUrl
             * @memberof presage.smartspectra.Landmarks
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Landmarks.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/presage.smartspectra.Landmarks";
            };

            return Landmarks;
        })();

        smartspectra.Face = (function() {

            /**
             * Properties of a Face.
             * @memberof presage.smartspectra
             * @interface IFace
             * @property {Array.<presage.smartspectra.IDetectionStatus>|null} [blinking] Blinking detection status over time
             * @property {Array.<presage.smartspectra.IDetectionStatus>|null} [talking] Talking/speech detection status over time
             * @property {Array.<presage.smartspectra.ILandmarks>|null} [landmarks] Facial landmark coordinates over time
             * @property {Array.<presage.smartspectra.IExpression>|null} [expression] Detected expressions over time
             */

            /**
             * Constructs a new Face.
             * @memberof presage.smartspectra
             * @classdesc Comprehensive facial analysis measurements and detections.
             * Contains blinking, talking detection, landmarks, and expressions.
             * @implements IFace
             * @constructor
             * @param {presage.smartspectra.IFace=} [properties] Properties to set
             */
            function Face(properties) {
                this.blinking = [];
                this.talking = [];
                this.landmarks = [];
                this.expression = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Blinking detection status over time
             * @member {Array.<presage.smartspectra.IDetectionStatus>} blinking
             * @memberof presage.smartspectra.Face
             * @instance
             */
            Face.prototype.blinking = $util.emptyArray;

            /**
             * Talking/speech detection status over time
             * @member {Array.<presage.smartspectra.IDetectionStatus>} talking
             * @memberof presage.smartspectra.Face
             * @instance
             */
            Face.prototype.talking = $util.emptyArray;

            /**
             * Facial landmark coordinates over time
             * @member {Array.<presage.smartspectra.ILandmarks>} landmarks
             * @memberof presage.smartspectra.Face
             * @instance
             */
            Face.prototype.landmarks = $util.emptyArray;

            /**
             * Detected expressions over time
             * @member {Array.<presage.smartspectra.IExpression>} expression
             * @memberof presage.smartspectra.Face
             * @instance
             */
            Face.prototype.expression = $util.emptyArray;

            /**
             * Creates a new Face instance using the specified properties.
             * @function create
             * @memberof presage.smartspectra.Face
             * @static
             * @param {presage.smartspectra.IFace=} [properties] Properties to set
             * @returns {presage.smartspectra.Face} Face instance
             */
            Face.create = function create(properties) {
                return new Face(properties);
            };

            /**
             * Encodes the specified Face message. Does not implicitly {@link presage.smartspectra.Face.verify|verify} messages.
             * @function encode
             * @memberof presage.smartspectra.Face
             * @static
             * @param {presage.smartspectra.IFace} message Face message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Face.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.blinking != null && message.blinking.length)
                    for (var i = 0; i < message.blinking.length; ++i)
                        $root.presage.smartspectra.DetectionStatus.encode(message.blinking[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.talking != null && message.talking.length)
                    for (var i = 0; i < message.talking.length; ++i)
                        $root.presage.smartspectra.DetectionStatus.encode(message.talking[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.landmarks != null && message.landmarks.length)
                    for (var i = 0; i < message.landmarks.length; ++i)
                        $root.presage.smartspectra.Landmarks.encode(message.landmarks[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.expression != null && message.expression.length)
                    for (var i = 0; i < message.expression.length; ++i)
                        $root.presage.smartspectra.Expression.encode(message.expression[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified Face message, length delimited. Does not implicitly {@link presage.smartspectra.Face.verify|verify} messages.
             * @function encodeDelimited
             * @memberof presage.smartspectra.Face
             * @static
             * @param {presage.smartspectra.IFace} message Face message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Face.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a Face message from the specified reader or buffer.
             * @function decode
             * @memberof presage.smartspectra.Face
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {presage.smartspectra.Face} Face
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Face.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = new $root.presage.smartspectra.Face();
                while (reader.pos < end) {
                    var tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            if (!(message.blinking && message.blinking.length))
                                message.blinking = [];
                            message.blinking.push($root.presage.smartspectra.DetectionStatus.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 2: {
                            if (!(message.talking && message.talking.length))
                                message.talking = [];
                            message.talking.push($root.presage.smartspectra.DetectionStatus.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 3: {
                            if (!(message.landmarks && message.landmarks.length))
                                message.landmarks = [];
                            message.landmarks.push($root.presage.smartspectra.Landmarks.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 4: {
                            if (!(message.expression && message.expression.length))
                                message.expression = [];
                            message.expression.push($root.presage.smartspectra.Expression.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Face message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof presage.smartspectra.Face
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {presage.smartspectra.Face} Face
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Face.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Face message.
             * @function verify
             * @memberof presage.smartspectra.Face
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Face.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.blinking != null && message.hasOwnProperty("blinking")) {
                    if (!Array.isArray(message.blinking))
                        return "blinking: array expected";
                    for (var i = 0; i < message.blinking.length; ++i) {
                        var error = $root.presage.smartspectra.DetectionStatus.verify(message.blinking[i], long + 1);
                        if (error)
                            return "blinking." + error;
                    }
                }
                if (message.talking != null && message.hasOwnProperty("talking")) {
                    if (!Array.isArray(message.talking))
                        return "talking: array expected";
                    for (var i = 0; i < message.talking.length; ++i) {
                        var error = $root.presage.smartspectra.DetectionStatus.verify(message.talking[i], long + 1);
                        if (error)
                            return "talking." + error;
                    }
                }
                if (message.landmarks != null && message.hasOwnProperty("landmarks")) {
                    if (!Array.isArray(message.landmarks))
                        return "landmarks: array expected";
                    for (var i = 0; i < message.landmarks.length; ++i) {
                        var error = $root.presage.smartspectra.Landmarks.verify(message.landmarks[i], long + 1);
                        if (error)
                            return "landmarks." + error;
                    }
                }
                if (message.expression != null && message.hasOwnProperty("expression")) {
                    if (!Array.isArray(message.expression))
                        return "expression: array expected";
                    for (var i = 0; i < message.expression.length; ++i) {
                        var error = $root.presage.smartspectra.Expression.verify(message.expression[i], long + 1);
                        if (error)
                            return "expression." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Face message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof presage.smartspectra.Face
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {presage.smartspectra.Face} Face
             */
            Face.fromObject = function fromObject(object, long) {
                if (object instanceof $root.presage.smartspectra.Face)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var message = new $root.presage.smartspectra.Face();
                if (object.blinking) {
                    if (!Array.isArray(object.blinking))
                        throw TypeError(".presage.smartspectra.Face.blinking: array expected");
                    message.blinking = [];
                    for (var i = 0; i < object.blinking.length; ++i) {
                        if (typeof object.blinking[i] !== "object")
                            throw TypeError(".presage.smartspectra.Face.blinking: object expected");
                        message.blinking[i] = $root.presage.smartspectra.DetectionStatus.fromObject(object.blinking[i], long + 1);
                    }
                }
                if (object.talking) {
                    if (!Array.isArray(object.talking))
                        throw TypeError(".presage.smartspectra.Face.talking: array expected");
                    message.talking = [];
                    for (var i = 0; i < object.talking.length; ++i) {
                        if (typeof object.talking[i] !== "object")
                            throw TypeError(".presage.smartspectra.Face.talking: object expected");
                        message.talking[i] = $root.presage.smartspectra.DetectionStatus.fromObject(object.talking[i], long + 1);
                    }
                }
                if (object.landmarks) {
                    if (!Array.isArray(object.landmarks))
                        throw TypeError(".presage.smartspectra.Face.landmarks: array expected");
                    message.landmarks = [];
                    for (var i = 0; i < object.landmarks.length; ++i) {
                        if (typeof object.landmarks[i] !== "object")
                            throw TypeError(".presage.smartspectra.Face.landmarks: object expected");
                        message.landmarks[i] = $root.presage.smartspectra.Landmarks.fromObject(object.landmarks[i], long + 1);
                    }
                }
                if (object.expression) {
                    if (!Array.isArray(object.expression))
                        throw TypeError(".presage.smartspectra.Face.expression: array expected");
                    message.expression = [];
                    for (var i = 0; i < object.expression.length; ++i) {
                        if (typeof object.expression[i] !== "object")
                            throw TypeError(".presage.smartspectra.Face.expression: object expected");
                        message.expression[i] = $root.presage.smartspectra.Expression.fromObject(object.expression[i], long + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a Face message. Also converts values to other types if specified.
             * @function toObject
             * @memberof presage.smartspectra.Face
             * @static
             * @param {presage.smartspectra.Face} message Face
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Face.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.blinking = [];
                    object.talking = [];
                    object.landmarks = [];
                    object.expression = [];
                }
                if (message.blinking && message.blinking.length) {
                    object.blinking = [];
                    for (var j = 0; j < message.blinking.length; ++j)
                        object.blinking[j] = $root.presage.smartspectra.DetectionStatus.toObject(message.blinking[j], options, q + 1);
                }
                if (message.talking && message.talking.length) {
                    object.talking = [];
                    for (var j = 0; j < message.talking.length; ++j)
                        object.talking[j] = $root.presage.smartspectra.DetectionStatus.toObject(message.talking[j], options, q + 1);
                }
                if (message.landmarks && message.landmarks.length) {
                    object.landmarks = [];
                    for (var j = 0; j < message.landmarks.length; ++j)
                        object.landmarks[j] = $root.presage.smartspectra.Landmarks.toObject(message.landmarks[j], options, q + 1);
                }
                if (message.expression && message.expression.length) {
                    object.expression = [];
                    for (var j = 0; j < message.expression.length; ++j)
                        object.expression[j] = $root.presage.smartspectra.Expression.toObject(message.expression[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this Face to JSON.
             * @function toJSON
             * @memberof presage.smartspectra.Face
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Face.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Face
             * @function getTypeUrl
             * @memberof presage.smartspectra.Face
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Face.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/presage.smartspectra.Face";
            };

            return Face;
        })();

        smartspectra.MicroMotion = (function() {

            /**
             * Properties of a MicroMotion.
             * @memberof presage.smartspectra
             * @interface IMicroMotion
             * @property {Array.<presage.smartspectra.IMeasurement>|null} [glutes] Micro-motion measurements in the glutes region
             * @property {Array.<presage.smartspectra.IMeasurement>|null} [knees] Micro-motion measurements in the knees region
             */

            /**
             * Constructs a new MicroMotion.
             * @memberof presage.smartspectra
             * @classdesc Micro-motion measurements for body movement analysis.
             * Tracks small movements in specific body regions.
             * @implements IMicroMotion
             * @constructor
             * @param {presage.smartspectra.IMicroMotion=} [properties] Properties to set
             */
            function MicroMotion(properties) {
                this.glutes = [];
                this.knees = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Micro-motion measurements in the glutes region
             * @member {Array.<presage.smartspectra.IMeasurement>} glutes
             * @memberof presage.smartspectra.MicroMotion
             * @instance
             */
            MicroMotion.prototype.glutes = $util.emptyArray;

            /**
             * Micro-motion measurements in the knees region
             * @member {Array.<presage.smartspectra.IMeasurement>} knees
             * @memberof presage.smartspectra.MicroMotion
             * @instance
             */
            MicroMotion.prototype.knees = $util.emptyArray;

            /**
             * Creates a new MicroMotion instance using the specified properties.
             * @function create
             * @memberof presage.smartspectra.MicroMotion
             * @static
             * @param {presage.smartspectra.IMicroMotion=} [properties] Properties to set
             * @returns {presage.smartspectra.MicroMotion} MicroMotion instance
             */
            MicroMotion.create = function create(properties) {
                return new MicroMotion(properties);
            };

            /**
             * Encodes the specified MicroMotion message. Does not implicitly {@link presage.smartspectra.MicroMotion.verify|verify} messages.
             * @function encode
             * @memberof presage.smartspectra.MicroMotion
             * @static
             * @param {presage.smartspectra.IMicroMotion} message MicroMotion message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MicroMotion.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.glutes != null && message.glutes.length)
                    for (var i = 0; i < message.glutes.length; ++i)
                        $root.presage.smartspectra.Measurement.encode(message.glutes[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.knees != null && message.knees.length)
                    for (var i = 0; i < message.knees.length; ++i)
                        $root.presage.smartspectra.Measurement.encode(message.knees[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified MicroMotion message, length delimited. Does not implicitly {@link presage.smartspectra.MicroMotion.verify|verify} messages.
             * @function encodeDelimited
             * @memberof presage.smartspectra.MicroMotion
             * @static
             * @param {presage.smartspectra.IMicroMotion} message MicroMotion message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MicroMotion.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a MicroMotion message from the specified reader or buffer.
             * @function decode
             * @memberof presage.smartspectra.MicroMotion
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {presage.smartspectra.MicroMotion} MicroMotion
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MicroMotion.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = new $root.presage.smartspectra.MicroMotion();
                while (reader.pos < end) {
                    var tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            if (!(message.glutes && message.glutes.length))
                                message.glutes = [];
                            message.glutes.push($root.presage.smartspectra.Measurement.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 2: {
                            if (!(message.knees && message.knees.length))
                                message.knees = [];
                            message.knees.push($root.presage.smartspectra.Measurement.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a MicroMotion message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof presage.smartspectra.MicroMotion
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {presage.smartspectra.MicroMotion} MicroMotion
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MicroMotion.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MicroMotion message.
             * @function verify
             * @memberof presage.smartspectra.MicroMotion
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MicroMotion.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.glutes != null && message.hasOwnProperty("glutes")) {
                    if (!Array.isArray(message.glutes))
                        return "glutes: array expected";
                    for (var i = 0; i < message.glutes.length; ++i) {
                        var error = $root.presage.smartspectra.Measurement.verify(message.glutes[i], long + 1);
                        if (error)
                            return "glutes." + error;
                    }
                }
                if (message.knees != null && message.hasOwnProperty("knees")) {
                    if (!Array.isArray(message.knees))
                        return "knees: array expected";
                    for (var i = 0; i < message.knees.length; ++i) {
                        var error = $root.presage.smartspectra.Measurement.verify(message.knees[i], long + 1);
                        if (error)
                            return "knees." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a MicroMotion message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof presage.smartspectra.MicroMotion
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {presage.smartspectra.MicroMotion} MicroMotion
             */
            MicroMotion.fromObject = function fromObject(object, long) {
                if (object instanceof $root.presage.smartspectra.MicroMotion)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var message = new $root.presage.smartspectra.MicroMotion();
                if (object.glutes) {
                    if (!Array.isArray(object.glutes))
                        throw TypeError(".presage.smartspectra.MicroMotion.glutes: array expected");
                    message.glutes = [];
                    for (var i = 0; i < object.glutes.length; ++i) {
                        if (typeof object.glutes[i] !== "object")
                            throw TypeError(".presage.smartspectra.MicroMotion.glutes: object expected");
                        message.glutes[i] = $root.presage.smartspectra.Measurement.fromObject(object.glutes[i], long + 1);
                    }
                }
                if (object.knees) {
                    if (!Array.isArray(object.knees))
                        throw TypeError(".presage.smartspectra.MicroMotion.knees: array expected");
                    message.knees = [];
                    for (var i = 0; i < object.knees.length; ++i) {
                        if (typeof object.knees[i] !== "object")
                            throw TypeError(".presage.smartspectra.MicroMotion.knees: object expected");
                        message.knees[i] = $root.presage.smartspectra.Measurement.fromObject(object.knees[i], long + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a MicroMotion message. Also converts values to other types if specified.
             * @function toObject
             * @memberof presage.smartspectra.MicroMotion
             * @static
             * @param {presage.smartspectra.MicroMotion} message MicroMotion
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MicroMotion.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.glutes = [];
                    object.knees = [];
                }
                if (message.glutes && message.glutes.length) {
                    object.glutes = [];
                    for (var j = 0; j < message.glutes.length; ++j)
                        object.glutes[j] = $root.presage.smartspectra.Measurement.toObject(message.glutes[j], options, q + 1);
                }
                if (message.knees && message.knees.length) {
                    object.knees = [];
                    for (var j = 0; j < message.knees.length; ++j)
                        object.knees[j] = $root.presage.smartspectra.Measurement.toObject(message.knees[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this MicroMotion to JSON.
             * @function toJSON
             * @memberof presage.smartspectra.MicroMotion
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MicroMotion.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MicroMotion
             * @function getTypeUrl
             * @memberof presage.smartspectra.MicroMotion
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MicroMotion.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/presage.smartspectra.MicroMotion";
            };

            return MicroMotion;
        })();

        smartspectra.Eda = (function() {

            /**
             * Properties of an Eda.
             * @memberof presage.smartspectra
             * @interface IEda
             * @property {Array.<presage.smartspectra.IMeasurement>|null} [trace] EDA trace measurements over time
             */

            /**
             * Constructs a new Eda.
             * @memberof presage.smartspectra
             * @classdesc Electrodermal Activity (EDA) measurements.
             * Tracks skin conductance changes related to autonomic nervous system activity.
             * @implements IEda
             * @constructor
             * @param {presage.smartspectra.IEda=} [properties] Properties to set
             */
            function Eda(properties) {
                this.trace = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * EDA trace measurements over time
             * @member {Array.<presage.smartspectra.IMeasurement>} trace
             * @memberof presage.smartspectra.Eda
             * @instance
             */
            Eda.prototype.trace = $util.emptyArray;

            /**
             * Creates a new Eda instance using the specified properties.
             * @function create
             * @memberof presage.smartspectra.Eda
             * @static
             * @param {presage.smartspectra.IEda=} [properties] Properties to set
             * @returns {presage.smartspectra.Eda} Eda instance
             */
            Eda.create = function create(properties) {
                return new Eda(properties);
            };

            /**
             * Encodes the specified Eda message. Does not implicitly {@link presage.smartspectra.Eda.verify|verify} messages.
             * @function encode
             * @memberof presage.smartspectra.Eda
             * @static
             * @param {presage.smartspectra.IEda} message Eda message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Eda.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.trace != null && message.trace.length)
                    for (var i = 0; i < message.trace.length; ++i)
                        $root.presage.smartspectra.Measurement.encode(message.trace[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified Eda message, length delimited. Does not implicitly {@link presage.smartspectra.Eda.verify|verify} messages.
             * @function encodeDelimited
             * @memberof presage.smartspectra.Eda
             * @static
             * @param {presage.smartspectra.IEda} message Eda message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Eda.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes an Eda message from the specified reader or buffer.
             * @function decode
             * @memberof presage.smartspectra.Eda
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {presage.smartspectra.Eda} Eda
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Eda.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = new $root.presage.smartspectra.Eda();
                while (reader.pos < end) {
                    var tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            if (!(message.trace && message.trace.length))
                                message.trace = [];
                            message.trace.push($root.presage.smartspectra.Measurement.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an Eda message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof presage.smartspectra.Eda
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {presage.smartspectra.Eda} Eda
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Eda.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an Eda message.
             * @function verify
             * @memberof presage.smartspectra.Eda
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Eda.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.trace != null && message.hasOwnProperty("trace")) {
                    if (!Array.isArray(message.trace))
                        return "trace: array expected";
                    for (var i = 0; i < message.trace.length; ++i) {
                        var error = $root.presage.smartspectra.Measurement.verify(message.trace[i], long + 1);
                        if (error)
                            return "trace." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an Eda message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof presage.smartspectra.Eda
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {presage.smartspectra.Eda} Eda
             */
            Eda.fromObject = function fromObject(object, long) {
                if (object instanceof $root.presage.smartspectra.Eda)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var message = new $root.presage.smartspectra.Eda();
                if (object.trace) {
                    if (!Array.isArray(object.trace))
                        throw TypeError(".presage.smartspectra.Eda.trace: array expected");
                    message.trace = [];
                    for (var i = 0; i < object.trace.length; ++i) {
                        if (typeof object.trace[i] !== "object")
                            throw TypeError(".presage.smartspectra.Eda.trace: object expected");
                        message.trace[i] = $root.presage.smartspectra.Measurement.fromObject(object.trace[i], long + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from an Eda message. Also converts values to other types if specified.
             * @function toObject
             * @memberof presage.smartspectra.Eda
             * @static
             * @param {presage.smartspectra.Eda} message Eda
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Eda.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.trace = [];
                if (message.trace && message.trace.length) {
                    object.trace = [];
                    for (var j = 0; j < message.trace.length; ++j)
                        object.trace[j] = $root.presage.smartspectra.Measurement.toObject(message.trace[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this Eda to JSON.
             * @function toJSON
             * @memberof presage.smartspectra.Eda
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Eda.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Eda
             * @function getTypeUrl
             * @memberof presage.smartspectra.Eda
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Eda.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/presage.smartspectra.Eda";
            };

            return Eda;
        })();

        smartspectra.Cardio = (function() {

            /**
             * Properties of a Cardio.
             * @memberof presage.smartspectra
             * @interface ICardio
             * @property {Array.<presage.smartspectra.IMeasurementWithConfidence>|null} [pulseRate] Heart rate measurements with confidence scores
             * @property {Array.<presage.smartspectra.IMeasurementWithConfidence>|null} [arterialPressureTrace] Arterial pressure trace (uncalibrated, unitless) measurements with confidence scores
             * @property {Array.<presage.smartspectra.IHrv>|null} [hrv] Heart rate variability measurements with confidence scores
             */

            /**
             * Constructs a new Cardio.
             * @memberof presage.smartspectra
             * @classdesc Represents a Cardio.
             * @implements ICardio
             * @constructor
             * @param {presage.smartspectra.ICardio=} [properties] Properties to set
             */
            function Cardio(properties) {
                this.pulseRate = [];
                this.arterialPressureTrace = [];
                this.hrv = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Heart rate measurements with confidence scores
             * @member {Array.<presage.smartspectra.IMeasurementWithConfidence>} pulseRate
             * @memberof presage.smartspectra.Cardio
             * @instance
             */
            Cardio.prototype.pulseRate = $util.emptyArray;

            /**
             * Arterial pressure trace (uncalibrated, unitless) measurements with confidence scores
             * @member {Array.<presage.smartspectra.IMeasurementWithConfidence>} arterialPressureTrace
             * @memberof presage.smartspectra.Cardio
             * @instance
             */
            Cardio.prototype.arterialPressureTrace = $util.emptyArray;

            /**
             * Heart rate variability measurements with confidence scores
             * @member {Array.<presage.smartspectra.IHrv>} hrv
             * @memberof presage.smartspectra.Cardio
             * @instance
             */
            Cardio.prototype.hrv = $util.emptyArray;

            /**
             * Creates a new Cardio instance using the specified properties.
             * @function create
             * @memberof presage.smartspectra.Cardio
             * @static
             * @param {presage.smartspectra.ICardio=} [properties] Properties to set
             * @returns {presage.smartspectra.Cardio} Cardio instance
             */
            Cardio.create = function create(properties) {
                return new Cardio(properties);
            };

            /**
             * Encodes the specified Cardio message. Does not implicitly {@link presage.smartspectra.Cardio.verify|verify} messages.
             * @function encode
             * @memberof presage.smartspectra.Cardio
             * @static
             * @param {presage.smartspectra.ICardio} message Cardio message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Cardio.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.pulseRate != null && message.pulseRate.length)
                    for (var i = 0; i < message.pulseRate.length; ++i)
                        $root.presage.smartspectra.MeasurementWithConfidence.encode(message.pulseRate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.arterialPressureTrace != null && message.arterialPressureTrace.length)
                    for (var i = 0; i < message.arterialPressureTrace.length; ++i)
                        $root.presage.smartspectra.MeasurementWithConfidence.encode(message.arterialPressureTrace[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.hrv != null && message.hrv.length)
                    for (var i = 0; i < message.hrv.length; ++i)
                        $root.presage.smartspectra.Hrv.encode(message.hrv[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified Cardio message, length delimited. Does not implicitly {@link presage.smartspectra.Cardio.verify|verify} messages.
             * @function encodeDelimited
             * @memberof presage.smartspectra.Cardio
             * @static
             * @param {presage.smartspectra.ICardio} message Cardio message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Cardio.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a Cardio message from the specified reader or buffer.
             * @function decode
             * @memberof presage.smartspectra.Cardio
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {presage.smartspectra.Cardio} Cardio
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Cardio.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = new $root.presage.smartspectra.Cardio();
                while (reader.pos < end) {
                    var tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            if (!(message.pulseRate && message.pulseRate.length))
                                message.pulseRate = [];
                            message.pulseRate.push($root.presage.smartspectra.MeasurementWithConfidence.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 2: {
                            if (!(message.arterialPressureTrace && message.arterialPressureTrace.length))
                                message.arterialPressureTrace = [];
                            message.arterialPressureTrace.push($root.presage.smartspectra.MeasurementWithConfidence.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 3: {
                            if (!(message.hrv && message.hrv.length))
                                message.hrv = [];
                            message.hrv.push($root.presage.smartspectra.Hrv.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Cardio message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof presage.smartspectra.Cardio
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {presage.smartspectra.Cardio} Cardio
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Cardio.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Cardio message.
             * @function verify
             * @memberof presage.smartspectra.Cardio
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Cardio.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.pulseRate != null && message.hasOwnProperty("pulseRate")) {
                    if (!Array.isArray(message.pulseRate))
                        return "pulseRate: array expected";
                    for (var i = 0; i < message.pulseRate.length; ++i) {
                        var error = $root.presage.smartspectra.MeasurementWithConfidence.verify(message.pulseRate[i], long + 1);
                        if (error)
                            return "pulseRate." + error;
                    }
                }
                if (message.arterialPressureTrace != null && message.hasOwnProperty("arterialPressureTrace")) {
                    if (!Array.isArray(message.arterialPressureTrace))
                        return "arterialPressureTrace: array expected";
                    for (var i = 0; i < message.arterialPressureTrace.length; ++i) {
                        var error = $root.presage.smartspectra.MeasurementWithConfidence.verify(message.arterialPressureTrace[i], long + 1);
                        if (error)
                            return "arterialPressureTrace." + error;
                    }
                }
                if (message.hrv != null && message.hasOwnProperty("hrv")) {
                    if (!Array.isArray(message.hrv))
                        return "hrv: array expected";
                    for (var i = 0; i < message.hrv.length; ++i) {
                        var error = $root.presage.smartspectra.Hrv.verify(message.hrv[i], long + 1);
                        if (error)
                            return "hrv." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Cardio message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof presage.smartspectra.Cardio
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {presage.smartspectra.Cardio} Cardio
             */
            Cardio.fromObject = function fromObject(object, long) {
                if (object instanceof $root.presage.smartspectra.Cardio)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var message = new $root.presage.smartspectra.Cardio();
                if (object.pulseRate) {
                    if (!Array.isArray(object.pulseRate))
                        throw TypeError(".presage.smartspectra.Cardio.pulseRate: array expected");
                    message.pulseRate = [];
                    for (var i = 0; i < object.pulseRate.length; ++i) {
                        if (typeof object.pulseRate[i] !== "object")
                            throw TypeError(".presage.smartspectra.Cardio.pulseRate: object expected");
                        message.pulseRate[i] = $root.presage.smartspectra.MeasurementWithConfidence.fromObject(object.pulseRate[i], long + 1);
                    }
                }
                if (object.arterialPressureTrace) {
                    if (!Array.isArray(object.arterialPressureTrace))
                        throw TypeError(".presage.smartspectra.Cardio.arterialPressureTrace: array expected");
                    message.arterialPressureTrace = [];
                    for (var i = 0; i < object.arterialPressureTrace.length; ++i) {
                        if (typeof object.arterialPressureTrace[i] !== "object")
                            throw TypeError(".presage.smartspectra.Cardio.arterialPressureTrace: object expected");
                        message.arterialPressureTrace[i] = $root.presage.smartspectra.MeasurementWithConfidence.fromObject(object.arterialPressureTrace[i], long + 1);
                    }
                }
                if (object.hrv) {
                    if (!Array.isArray(object.hrv))
                        throw TypeError(".presage.smartspectra.Cardio.hrv: array expected");
                    message.hrv = [];
                    for (var i = 0; i < object.hrv.length; ++i) {
                        if (typeof object.hrv[i] !== "object")
                            throw TypeError(".presage.smartspectra.Cardio.hrv: object expected");
                        message.hrv[i] = $root.presage.smartspectra.Hrv.fromObject(object.hrv[i], long + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a Cardio message. Also converts values to other types if specified.
             * @function toObject
             * @memberof presage.smartspectra.Cardio
             * @static
             * @param {presage.smartspectra.Cardio} message Cardio
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Cardio.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.pulseRate = [];
                    object.arterialPressureTrace = [];
                    object.hrv = [];
                }
                if (message.pulseRate && message.pulseRate.length) {
                    object.pulseRate = [];
                    for (var j = 0; j < message.pulseRate.length; ++j)
                        object.pulseRate[j] = $root.presage.smartspectra.MeasurementWithConfidence.toObject(message.pulseRate[j], options, q + 1);
                }
                if (message.arterialPressureTrace && message.arterialPressureTrace.length) {
                    object.arterialPressureTrace = [];
                    for (var j = 0; j < message.arterialPressureTrace.length; ++j)
                        object.arterialPressureTrace[j] = $root.presage.smartspectra.MeasurementWithConfidence.toObject(message.arterialPressureTrace[j], options, q + 1);
                }
                if (message.hrv && message.hrv.length) {
                    object.hrv = [];
                    for (var j = 0; j < message.hrv.length; ++j)
                        object.hrv[j] = $root.presage.smartspectra.Hrv.toObject(message.hrv[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this Cardio to JSON.
             * @function toJSON
             * @memberof presage.smartspectra.Cardio
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Cardio.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Cardio
             * @function getTypeUrl
             * @memberof presage.smartspectra.Cardio
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Cardio.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/presage.smartspectra.Cardio";
            };

            return Cardio;
        })();

        smartspectra.Metrics = (function() {

            /**
             * Properties of a Metrics.
             * @memberof presage.smartspectra
             * @interface IMetrics
             * @property {presage.smartspectra.IBreathing|null} [breathing] Breathing and respiratory analysis results
             * @property {presage.smartspectra.IMicroMotion|null} [micromotion] Micro-motion analysis results
             * @property {presage.smartspectra.IEda|null} [eda] Electrodermal activity measurements. Note: processing needs to run for over 35 seconds to generate the first EDA result.
             * @property {presage.smartspectra.IFace|null} [face] Facial analysis results
             * @property {presage.smartspectra.ICardio|null} [cardio] Cardiovascular measurements (pulse rate, arterial pressure, HRV)
             */

            /**
             * Constructs a new Metrics.
             * @memberof presage.smartspectra
             * @classdesc Comprehensive physiological metrics container.
             * Contains all available physiological measurements and analysis results.
             * @implements IMetrics
             * @constructor
             * @param {presage.smartspectra.IMetrics=} [properties] Properties to set
             */
            function Metrics(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Breathing and respiratory analysis results
             * @member {presage.smartspectra.IBreathing|null|undefined} breathing
             * @memberof presage.smartspectra.Metrics
             * @instance
             */
            Metrics.prototype.breathing = null;

            /**
             * Micro-motion analysis results
             * @member {presage.smartspectra.IMicroMotion|null|undefined} micromotion
             * @memberof presage.smartspectra.Metrics
             * @instance
             */
            Metrics.prototype.micromotion = null;

            /**
             * Electrodermal activity measurements. Note: processing needs to run for over 35 seconds to generate the first EDA result.
             * @member {presage.smartspectra.IEda|null|undefined} eda
             * @memberof presage.smartspectra.Metrics
             * @instance
             */
            Metrics.prototype.eda = null;

            /**
             * Facial analysis results
             * @member {presage.smartspectra.IFace|null|undefined} face
             * @memberof presage.smartspectra.Metrics
             * @instance
             */
            Metrics.prototype.face = null;

            /**
             * Cardiovascular measurements (pulse rate, arterial pressure, HRV)
             * @member {presage.smartspectra.ICardio|null|undefined} cardio
             * @memberof presage.smartspectra.Metrics
             * @instance
             */
            Metrics.prototype.cardio = null;

            /**
             * Creates a new Metrics instance using the specified properties.
             * @function create
             * @memberof presage.smartspectra.Metrics
             * @static
             * @param {presage.smartspectra.IMetrics=} [properties] Properties to set
             * @returns {presage.smartspectra.Metrics} Metrics instance
             */
            Metrics.create = function create(properties) {
                return new Metrics(properties);
            };

            /**
             * Encodes the specified Metrics message. Does not implicitly {@link presage.smartspectra.Metrics.verify|verify} messages.
             * @function encode
             * @memberof presage.smartspectra.Metrics
             * @static
             * @param {presage.smartspectra.IMetrics} message Metrics message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Metrics.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.breathing != null && Object.hasOwnProperty.call(message, "breathing"))
                    $root.presage.smartspectra.Breathing.encode(message.breathing, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.micromotion != null && Object.hasOwnProperty.call(message, "micromotion"))
                    $root.presage.smartspectra.MicroMotion.encode(message.micromotion, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.eda != null && Object.hasOwnProperty.call(message, "eda"))
                    $root.presage.smartspectra.Eda.encode(message.eda, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.face != null && Object.hasOwnProperty.call(message, "face"))
                    $root.presage.smartspectra.Face.encode(message.face, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.cardio != null && Object.hasOwnProperty.call(message, "cardio"))
                    $root.presage.smartspectra.Cardio.encode(message.cardio, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified Metrics message, length delimited. Does not implicitly {@link presage.smartspectra.Metrics.verify|verify} messages.
             * @function encodeDelimited
             * @memberof presage.smartspectra.Metrics
             * @static
             * @param {presage.smartspectra.IMetrics} message Metrics message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Metrics.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a Metrics message from the specified reader or buffer.
             * @function decode
             * @memberof presage.smartspectra.Metrics
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {presage.smartspectra.Metrics} Metrics
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Metrics.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = new $root.presage.smartspectra.Metrics();
                while (reader.pos < end) {
                    var tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.breathing = $root.presage.smartspectra.Breathing.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 2: {
                            message.micromotion = $root.presage.smartspectra.MicroMotion.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 3: {
                            message.eda = $root.presage.smartspectra.Eda.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 4: {
                            message.face = $root.presage.smartspectra.Face.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 5: {
                            message.cardio = $root.presage.smartspectra.Cardio.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Metrics message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof presage.smartspectra.Metrics
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {presage.smartspectra.Metrics} Metrics
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Metrics.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Metrics message.
             * @function verify
             * @memberof presage.smartspectra.Metrics
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Metrics.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.breathing != null && message.hasOwnProperty("breathing")) {
                    var error = $root.presage.smartspectra.Breathing.verify(message.breathing, long + 1);
                    if (error)
                        return "breathing." + error;
                }
                if (message.micromotion != null && message.hasOwnProperty("micromotion")) {
                    var error = $root.presage.smartspectra.MicroMotion.verify(message.micromotion, long + 1);
                    if (error)
                        return "micromotion." + error;
                }
                if (message.eda != null && message.hasOwnProperty("eda")) {
                    var error = $root.presage.smartspectra.Eda.verify(message.eda, long + 1);
                    if (error)
                        return "eda." + error;
                }
                if (message.face != null && message.hasOwnProperty("face")) {
                    var error = $root.presage.smartspectra.Face.verify(message.face, long + 1);
                    if (error)
                        return "face." + error;
                }
                if (message.cardio != null && message.hasOwnProperty("cardio")) {
                    var error = $root.presage.smartspectra.Cardio.verify(message.cardio, long + 1);
                    if (error)
                        return "cardio." + error;
                }
                return null;
            };

            /**
             * Creates a Metrics message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof presage.smartspectra.Metrics
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {presage.smartspectra.Metrics} Metrics
             */
            Metrics.fromObject = function fromObject(object, long) {
                if (object instanceof $root.presage.smartspectra.Metrics)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var message = new $root.presage.smartspectra.Metrics();
                if (object.breathing != null) {
                    if (typeof object.breathing !== "object")
                        throw TypeError(".presage.smartspectra.Metrics.breathing: object expected");
                    message.breathing = $root.presage.smartspectra.Breathing.fromObject(object.breathing, long + 1);
                }
                if (object.micromotion != null) {
                    if (typeof object.micromotion !== "object")
                        throw TypeError(".presage.smartspectra.Metrics.micromotion: object expected");
                    message.micromotion = $root.presage.smartspectra.MicroMotion.fromObject(object.micromotion, long + 1);
                }
                if (object.eda != null) {
                    if (typeof object.eda !== "object")
                        throw TypeError(".presage.smartspectra.Metrics.eda: object expected");
                    message.eda = $root.presage.smartspectra.Eda.fromObject(object.eda, long + 1);
                }
                if (object.face != null) {
                    if (typeof object.face !== "object")
                        throw TypeError(".presage.smartspectra.Metrics.face: object expected");
                    message.face = $root.presage.smartspectra.Face.fromObject(object.face, long + 1);
                }
                if (object.cardio != null) {
                    if (typeof object.cardio !== "object")
                        throw TypeError(".presage.smartspectra.Metrics.cardio: object expected");
                    message.cardio = $root.presage.smartspectra.Cardio.fromObject(object.cardio, long + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a Metrics message. Also converts values to other types if specified.
             * @function toObject
             * @memberof presage.smartspectra.Metrics
             * @static
             * @param {presage.smartspectra.Metrics} message Metrics
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Metrics.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.breathing = null;
                    object.micromotion = null;
                    object.eda = null;
                    object.face = null;
                    object.cardio = null;
                }
                if (message.breathing != null && message.hasOwnProperty("breathing"))
                    object.breathing = $root.presage.smartspectra.Breathing.toObject(message.breathing, options, q + 1);
                if (message.micromotion != null && message.hasOwnProperty("micromotion"))
                    object.micromotion = $root.presage.smartspectra.MicroMotion.toObject(message.micromotion, options, q + 1);
                if (message.eda != null && message.hasOwnProperty("eda"))
                    object.eda = $root.presage.smartspectra.Eda.toObject(message.eda, options, q + 1);
                if (message.face != null && message.hasOwnProperty("face"))
                    object.face = $root.presage.smartspectra.Face.toObject(message.face, options, q + 1);
                if (message.cardio != null && message.hasOwnProperty("cardio"))
                    object.cardio = $root.presage.smartspectra.Cardio.toObject(message.cardio, options, q + 1);
                return object;
            };

            /**
             * Converts this Metrics to JSON.
             * @function toJSON
             * @memberof presage.smartspectra.Metrics
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Metrics.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Metrics
             * @function getTypeUrl
             * @memberof presage.smartspectra.Metrics
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Metrics.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/presage.smartspectra.Metrics";
            };

            return Metrics;
        })();

        smartspectra.Point2dInt32 = (function() {

            /**
             * Properties of a Point2dInt32.
             * @memberof presage.smartspectra
             * @interface IPoint2dInt32
             * @property {number|null} [x] X coordinate as 32-bit signed integer
             * @property {number|null} [y] Y coordinate as 32-bit signed integer
             */

            /**
             * Constructs a new Point2dInt32.
             * @memberof presage.smartspectra
             * @classdesc Represents a 2D point with integer coordinates.
             * Used for pixel-based coordinate systems and discrete positioning.
             * @implements IPoint2dInt32
             * @constructor
             * @param {presage.smartspectra.IPoint2dInt32=} [properties] Properties to set
             */
            function Point2dInt32(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * X coordinate as 32-bit signed integer
             * @member {number} x
             * @memberof presage.smartspectra.Point2dInt32
             * @instance
             */
            Point2dInt32.prototype.x = 0;

            /**
             * Y coordinate as 32-bit signed integer
             * @member {number} y
             * @memberof presage.smartspectra.Point2dInt32
             * @instance
             */
            Point2dInt32.prototype.y = 0;

            /**
             * Creates a new Point2dInt32 instance using the specified properties.
             * @function create
             * @memberof presage.smartspectra.Point2dInt32
             * @static
             * @param {presage.smartspectra.IPoint2dInt32=} [properties] Properties to set
             * @returns {presage.smartspectra.Point2dInt32} Point2dInt32 instance
             */
            Point2dInt32.create = function create(properties) {
                return new Point2dInt32(properties);
            };

            /**
             * Encodes the specified Point2dInt32 message. Does not implicitly {@link presage.smartspectra.Point2dInt32.verify|verify} messages.
             * @function encode
             * @memberof presage.smartspectra.Point2dInt32
             * @static
             * @param {presage.smartspectra.IPoint2dInt32} message Point2dInt32 message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Point2dInt32.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.x != null && Object.hasOwnProperty.call(message, "x"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.x);
                if (message.y != null && Object.hasOwnProperty.call(message, "y"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.y);
                return writer;
            };

            /**
             * Encodes the specified Point2dInt32 message, length delimited. Does not implicitly {@link presage.smartspectra.Point2dInt32.verify|verify} messages.
             * @function encodeDelimited
             * @memberof presage.smartspectra.Point2dInt32
             * @static
             * @param {presage.smartspectra.IPoint2dInt32} message Point2dInt32 message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Point2dInt32.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a Point2dInt32 message from the specified reader or buffer.
             * @function decode
             * @memberof presage.smartspectra.Point2dInt32
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {presage.smartspectra.Point2dInt32} Point2dInt32
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Point2dInt32.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = new $root.presage.smartspectra.Point2dInt32();
                while (reader.pos < end) {
                    var tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.x = reader.int32();
                            break;
                        }
                    case 2: {
                            message.y = reader.int32();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Point2dInt32 message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof presage.smartspectra.Point2dInt32
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {presage.smartspectra.Point2dInt32} Point2dInt32
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Point2dInt32.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Point2dInt32 message.
             * @function verify
             * @memberof presage.smartspectra.Point2dInt32
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Point2dInt32.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.x != null && message.hasOwnProperty("x"))
                    if (!$util.isInteger(message.x))
                        return "x: integer expected";
                if (message.y != null && message.hasOwnProperty("y"))
                    if (!$util.isInteger(message.y))
                        return "y: integer expected";
                return null;
            };

            /**
             * Creates a Point2dInt32 message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof presage.smartspectra.Point2dInt32
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {presage.smartspectra.Point2dInt32} Point2dInt32
             */
            Point2dInt32.fromObject = function fromObject(object, long) {
                if (object instanceof $root.presage.smartspectra.Point2dInt32)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var message = new $root.presage.smartspectra.Point2dInt32();
                if (object.x != null)
                    message.x = object.x | 0;
                if (object.y != null)
                    message.y = object.y | 0;
                return message;
            };

            /**
             * Creates a plain object from a Point2dInt32 message. Also converts values to other types if specified.
             * @function toObject
             * @memberof presage.smartspectra.Point2dInt32
             * @static
             * @param {presage.smartspectra.Point2dInt32} message Point2dInt32
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Point2dInt32.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.x = 0;
                    object.y = 0;
                }
                if (message.x != null && message.hasOwnProperty("x"))
                    object.x = message.x;
                if (message.y != null && message.hasOwnProperty("y"))
                    object.y = message.y;
                return object;
            };

            /**
             * Converts this Point2dInt32 to JSON.
             * @function toJSON
             * @memberof presage.smartspectra.Point2dInt32
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Point2dInt32.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Point2dInt32
             * @function getTypeUrl
             * @memberof presage.smartspectra.Point2dInt32
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Point2dInt32.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/presage.smartspectra.Point2dInt32";
            };

            return Point2dInt32;
        })();

        smartspectra.Point2dFloat = (function() {

            /**
             * Properties of a Point2dFloat.
             * @memberof presage.smartspectra
             * @interface IPoint2dFloat
             * @property {number|null} [x] X coordinate as floating-point value
             * @property {number|null} [y] Y coordinate as floating-point value
             */

            /**
             * Constructs a new Point2dFloat.
             * @memberof presage.smartspectra
             * @classdesc Represents a 2D point with floating-point coordinates.
             * Used for precise positioning, normalized coordinates, and sub-pixel accuracy.
             * @implements IPoint2dFloat
             * @constructor
             * @param {presage.smartspectra.IPoint2dFloat=} [properties] Properties to set
             */
            function Point2dFloat(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * X coordinate as floating-point value
             * @member {number} x
             * @memberof presage.smartspectra.Point2dFloat
             * @instance
             */
            Point2dFloat.prototype.x = 0;

            /**
             * Y coordinate as floating-point value
             * @member {number} y
             * @memberof presage.smartspectra.Point2dFloat
             * @instance
             */
            Point2dFloat.prototype.y = 0;

            /**
             * Creates a new Point2dFloat instance using the specified properties.
             * @function create
             * @memberof presage.smartspectra.Point2dFloat
             * @static
             * @param {presage.smartspectra.IPoint2dFloat=} [properties] Properties to set
             * @returns {presage.smartspectra.Point2dFloat} Point2dFloat instance
             */
            Point2dFloat.create = function create(properties) {
                return new Point2dFloat(properties);
            };

            /**
             * Encodes the specified Point2dFloat message. Does not implicitly {@link presage.smartspectra.Point2dFloat.verify|verify} messages.
             * @function encode
             * @memberof presage.smartspectra.Point2dFloat
             * @static
             * @param {presage.smartspectra.IPoint2dFloat} message Point2dFloat message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Point2dFloat.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.x != null && Object.hasOwnProperty.call(message, "x"))
                    writer.uint32(/* id 1, wireType 5 =*/13).float(message.x);
                if (message.y != null && Object.hasOwnProperty.call(message, "y"))
                    writer.uint32(/* id 2, wireType 5 =*/21).float(message.y);
                return writer;
            };

            /**
             * Encodes the specified Point2dFloat message, length delimited. Does not implicitly {@link presage.smartspectra.Point2dFloat.verify|verify} messages.
             * @function encodeDelimited
             * @memberof presage.smartspectra.Point2dFloat
             * @static
             * @param {presage.smartspectra.IPoint2dFloat} message Point2dFloat message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Point2dFloat.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a Point2dFloat message from the specified reader or buffer.
             * @function decode
             * @memberof presage.smartspectra.Point2dFloat
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {presage.smartspectra.Point2dFloat} Point2dFloat
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Point2dFloat.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = new $root.presage.smartspectra.Point2dFloat();
                while (reader.pos < end) {
                    var tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.x = reader.float();
                            break;
                        }
                    case 2: {
                            message.y = reader.float();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Point2dFloat message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof presage.smartspectra.Point2dFloat
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {presage.smartspectra.Point2dFloat} Point2dFloat
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Point2dFloat.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Point2dFloat message.
             * @function verify
             * @memberof presage.smartspectra.Point2dFloat
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Point2dFloat.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.x != null && message.hasOwnProperty("x"))
                    if (typeof message.x !== "number")
                        return "x: number expected";
                if (message.y != null && message.hasOwnProperty("y"))
                    if (typeof message.y !== "number")
                        return "y: number expected";
                return null;
            };

            /**
             * Creates a Point2dFloat message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof presage.smartspectra.Point2dFloat
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {presage.smartspectra.Point2dFloat} Point2dFloat
             */
            Point2dFloat.fromObject = function fromObject(object, long) {
                if (object instanceof $root.presage.smartspectra.Point2dFloat)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var message = new $root.presage.smartspectra.Point2dFloat();
                if (object.x != null)
                    message.x = Number(object.x);
                if (object.y != null)
                    message.y = Number(object.y);
                return message;
            };

            /**
             * Creates a plain object from a Point2dFloat message. Also converts values to other types if specified.
             * @function toObject
             * @memberof presage.smartspectra.Point2dFloat
             * @static
             * @param {presage.smartspectra.Point2dFloat} message Point2dFloat
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Point2dFloat.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.x = 0;
                    object.y = 0;
                }
                if (message.x != null && message.hasOwnProperty("x"))
                    object.x = options.json && !isFinite(message.x) ? String(message.x) : message.x;
                if (message.y != null && message.hasOwnProperty("y"))
                    object.y = options.json && !isFinite(message.y) ? String(message.y) : message.y;
                return object;
            };

            /**
             * Converts this Point2dFloat to JSON.
             * @function toJSON
             * @memberof presage.smartspectra.Point2dFloat
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Point2dFloat.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Point2dFloat
             * @function getTypeUrl
             * @memberof presage.smartspectra.Point2dFloat
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Point2dFloat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/presage.smartspectra.Point2dFloat";
            };

            return Point2dFloat;
        })();

        smartspectra.Point3dFloat = (function() {

            /**
             * Properties of a Point3dFloat.
             * @memberof presage.smartspectra
             * @interface IPoint3dFloat
             * @property {number|null} [x] X coordinate as floating-point value
             * @property {number|null} [y] Y coordinate as floating-point value
             * @property {number|null} [z] Z coordinate (depth) as floating-point value
             */

            /**
             * Constructs a new Point3dFloat.
             * @memberof presage.smartspectra
             * @classdesc Represents a 3D point with floating-point coordinates.
             * Used for spatial positioning, depth information, and 3D landmark representation.
             * @implements IPoint3dFloat
             * @constructor
             * @param {presage.smartspectra.IPoint3dFloat=} [properties] Properties to set
             */
            function Point3dFloat(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * X coordinate as floating-point value
             * @member {number} x
             * @memberof presage.smartspectra.Point3dFloat
             * @instance
             */
            Point3dFloat.prototype.x = 0;

            /**
             * Y coordinate as floating-point value
             * @member {number} y
             * @memberof presage.smartspectra.Point3dFloat
             * @instance
             */
            Point3dFloat.prototype.y = 0;

            /**
             * Z coordinate (depth) as floating-point value
             * @member {number} z
             * @memberof presage.smartspectra.Point3dFloat
             * @instance
             */
            Point3dFloat.prototype.z = 0;

            /**
             * Creates a new Point3dFloat instance using the specified properties.
             * @function create
             * @memberof presage.smartspectra.Point3dFloat
             * @static
             * @param {presage.smartspectra.IPoint3dFloat=} [properties] Properties to set
             * @returns {presage.smartspectra.Point3dFloat} Point3dFloat instance
             */
            Point3dFloat.create = function create(properties) {
                return new Point3dFloat(properties);
            };

            /**
             * Encodes the specified Point3dFloat message. Does not implicitly {@link presage.smartspectra.Point3dFloat.verify|verify} messages.
             * @function encode
             * @memberof presage.smartspectra.Point3dFloat
             * @static
             * @param {presage.smartspectra.IPoint3dFloat} message Point3dFloat message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Point3dFloat.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.x != null && Object.hasOwnProperty.call(message, "x"))
                    writer.uint32(/* id 1, wireType 5 =*/13).float(message.x);
                if (message.y != null && Object.hasOwnProperty.call(message, "y"))
                    writer.uint32(/* id 2, wireType 5 =*/21).float(message.y);
                if (message.z != null && Object.hasOwnProperty.call(message, "z"))
                    writer.uint32(/* id 3, wireType 5 =*/29).float(message.z);
                return writer;
            };

            /**
             * Encodes the specified Point3dFloat message, length delimited. Does not implicitly {@link presage.smartspectra.Point3dFloat.verify|verify} messages.
             * @function encodeDelimited
             * @memberof presage.smartspectra.Point3dFloat
             * @static
             * @param {presage.smartspectra.IPoint3dFloat} message Point3dFloat message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Point3dFloat.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes a Point3dFloat message from the specified reader or buffer.
             * @function decode
             * @memberof presage.smartspectra.Point3dFloat
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {presage.smartspectra.Point3dFloat} Point3dFloat
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Point3dFloat.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = new $root.presage.smartspectra.Point3dFloat();
                while (reader.pos < end) {
                    var tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.x = reader.float();
                            break;
                        }
                    case 2: {
                            message.y = reader.float();
                            break;
                        }
                    case 3: {
                            message.z = reader.float();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Point3dFloat message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof presage.smartspectra.Point3dFloat
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {presage.smartspectra.Point3dFloat} Point3dFloat
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Point3dFloat.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Point3dFloat message.
             * @function verify
             * @memberof presage.smartspectra.Point3dFloat
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Point3dFloat.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.x != null && message.hasOwnProperty("x"))
                    if (typeof message.x !== "number")
                        return "x: number expected";
                if (message.y != null && message.hasOwnProperty("y"))
                    if (typeof message.y !== "number")
                        return "y: number expected";
                if (message.z != null && message.hasOwnProperty("z"))
                    if (typeof message.z !== "number")
                        return "z: number expected";
                return null;
            };

            /**
             * Creates a Point3dFloat message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof presage.smartspectra.Point3dFloat
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {presage.smartspectra.Point3dFloat} Point3dFloat
             */
            Point3dFloat.fromObject = function fromObject(object, long) {
                if (object instanceof $root.presage.smartspectra.Point3dFloat)
                    return object;
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var message = new $root.presage.smartspectra.Point3dFloat();
                if (object.x != null)
                    message.x = Number(object.x);
                if (object.y != null)
                    message.y = Number(object.y);
                if (object.z != null)
                    message.z = Number(object.z);
                return message;
            };

            /**
             * Creates a plain object from a Point3dFloat message. Also converts values to other types if specified.
             * @function toObject
             * @memberof presage.smartspectra.Point3dFloat
             * @static
             * @param {presage.smartspectra.Point3dFloat} message Point3dFloat
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Point3dFloat.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.x = 0;
                    object.y = 0;
                    object.z = 0;
                }
                if (message.x != null && message.hasOwnProperty("x"))
                    object.x = options.json && !isFinite(message.x) ? String(message.x) : message.x;
                if (message.y != null && message.hasOwnProperty("y"))
                    object.y = options.json && !isFinite(message.y) ? String(message.y) : message.y;
                if (message.z != null && message.hasOwnProperty("z"))
                    object.z = options.json && !isFinite(message.z) ? String(message.z) : message.z;
                return object;
            };

            /**
             * Converts this Point3dFloat to JSON.
             * @function toJSON
             * @memberof presage.smartspectra.Point3dFloat
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Point3dFloat.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Point3dFloat
             * @function getTypeUrl
             * @memberof presage.smartspectra.Point3dFloat
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Point3dFloat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/presage.smartspectra.Point3dFloat";
            };

            return Point3dFloat;
        })();

        return smartspectra;
    })();

    return presage;
})();

module.exports = $root;
